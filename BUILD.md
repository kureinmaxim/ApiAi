# ⚙️ Сборка и релиз ApiAi

Этот документ описывает процесс подготовки релиза ApiAi.

> **Примечание:** ApiAi — это Python приложение без инсталлятора. Пользователи запускают его через `python main.py` после установки зависимостей.

---

## 🔄 1. Управление версиями

В проекте используется **централизованная система версий**.  
Единственный источник правды — это `config/config_qt.json.template`.

### 📂 Где хранится версия?
- **Template:** `config/config_qt.json.template`
- **Локальный:** `config_qt.json` (не в Git)

> ⚠️ **Важно:** Никогда не меняйте версию вручную. Используйте `scripts/update_version.py`.

### 🛠 Пошаговый процесс обновления

#### Шаг 1: Проверка текущего статуса

```bash
python scripts/update_version.py status
```

#### Шаг 2: Обновление версии

```bash
# Patch (1.0.0 → 1.0.1)
python scripts/update_version.py bump --type patch

# Minor (1.0.0 → 1.1.0)
python scripts/update_version.py bump --type minor

# Major (1.0.0 → 2.0.0)
python scripts/update_version.py bump --type major

# Или установить конкретную версию
python scripts/update_version.py set 1.2.0
```

#### Шаг 3: Синхронизация

```bash
python scripts/update_version.py sync
```

**Что делает sync:**
1. Обновляет `config_qt.json` (только `app_info`)
2. Сохраняет персональные настройки (API keys, theme, window size)

---

## 📦 2. Подготовка релиза

ApiAi не имеет инсталлятора - это Python приложение, которое запускается из исходников.

### Шаг 1: Убедитесь что всё работает

```bash
# Проверка версии
python scripts/update_version.py status

# Проверка зависимостей
pip install -r requirements.txt

# Запуск приложения
python main.py
```

### Шаг 2: Подготовка офлайн пакетов (опционально)

Для пользователей без интернета:

```bash
# Скачать все зависимости
pip download -r requirements.txt -d offline_packages

# Для конкретной платформы (Windows)
pip download -r requirements.txt \
    -d offline_packages \
    --platform win_amd64 \
    --python-version 38 \
    --only-binary=:all:
```

### Шаг 3: Проверка шрифтов

Для корректного отображения кириллицы в PDF:

```bash
# Проверить наличие шрифтов
ls fonts/

# Должны быть:
# - DejaVuSans.ttf
# - DejaVuSans-Bold.ttf

# Если нет, скачать
python scripts/download_fonts.py
```

---

## 🚀 3. Публикация релиза

### Workflow для GitHub Release

#### 1. Подготовка

```bash
# Убедитесь что всё закоммичено
git status

# Проверьте версию
python scripts/update_version.py status
```

#### 2. Обновление версии

```bash
# Обновить версию
python scripts/update_version.py bump --type minor

# Синхронизировать
python scripts/update_version.py sync
```

#### 3. Git Commit & Tag

```bash
# Закоммитить изменения
git add config/config_qt.json.template
git commit -m "Release: v1.1.0"

# Создать тег
git tag v1.1.0

# Отправить в GitHub
git push origin main --tags
```

#### 4. GitHub Release

1. Перейдите на GitHub → Releases → New Release
2. Выберите тег `v1.1.0`
3. Название: `ApiAi v1.1.0`
4. Описание: из CHANGELOG или вручную
5. Приложите архив с кодом (опционально - GitHub сделает сам)
6. Приложите `offline_packages` (если подготовили)

---

## 📋 Чек-лист релиза

Перед публикацией убедитесь:

- [ ] Версия обновлена через `update_version.py`
- [ ] Все изменения закоммичены
- [ ] `python main.py` запускается без ошибок
- [ ] Шрифты DejaVu Sans в `fonts/`
- [ ] `requirements.txt` актуален
- [ ] CHANGELOG.md обновлен (если есть)
- [ ] Тег создан: `git tag v1.x.x`
- [ ] Pushed to GitHub: `git push --tags`

---

## 📚 Дистрибуция

### Формат распространения

ApiAi распространяется как:

1. **Исходный код на GitHub**
   - Пользователи клонируют и запускают через Python

2. **ZIP архив** (опционально)
   - Исходный код
   - `offline_packages/` с зависимостями
   - `README.md` с инструкциями

### Инструкция для пользователей

Добавьте в Release description:

```markdown
## Установка

1. Скачайте и распакуйте архив
2. Установите Python 3.8+ (если нет)
3. Откройте terminal в папке ApiAi
4. Создайте виртуальное окружение:
   ```
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   source .venv/bin/activate  # macOS/Linux
   ```
5. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```
6. Запустите:
   ```
   python main.py
   ```

## Офлайн установка

Если нет интернета:
```
pip install --no-index --find-links=offline_packages -r requirements.txt
```
```

---

## 🐛 Устранение неполадок

### ❌ Версии рассинхронизировались

Если `status` показывает предупреждения:

```bash
python scripts/update_version.py sync
```

### ❌ Зависимости не устанавливаются

```bash
# Обновите pip
pip install --upgrade pip

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### ❌ Шрифты не найдены

```bash
# Скачать заново
python scripts/download_fonts.py

# Или скопировать из другого проекта
cp /path/to/BOMCategorizer/fonts/*.ttf fonts/
```

---

## 🔧 Инструменты разработчика

### Проверка версии

```bash
python scripts/update_version.py status
```

### Обновление версии

```bash
# Автоинкремент
python scripts/update_version.py bump --type patch

# Конкретная версия
python scripts/update_version.py set 1.5.0
```

### Синхронизация

```bash
python scripts/update_version.py sync
```

---

## 📝 История изменений

Ведите CHANGELOG.md для отслеживания изменений между версиями:

```markdown
# Changelog

## [1.1.0] - 2025-12-01
### Added
- PDF export with Cyrillic support
- Word document export

### Fixed
- Font path detection for macOS

## [1.0.0] - 2025-11-30
### Added
- Initial release
- Three AI providers support
- Encryption for Telegram Bot API
```

---

**Дата создания:** 30.11.2025  
**Автор:** Kurein M.N.  
**Версия документа:** 1.0
