# 🏗 Структура Rust проекта

Этот документ описывает структуру файлов и папок экспериментальной версии ApiAi на Rust.

## 📂 Корневая папка (`rust/`)

### 📄 Конфигурация и Сборка

- **`Cargo.toml`**
  - **Назначение:** Главный файл манифеста проекта Rust.
  - **Содержание:** Метаданные проекта (имя, версия, авторы) и список зависимостей (crates), необходимых для сборки (например, `eframe`, `reqwest`, `serde`).

- **`Cargo.lock`**
  - **Назначение:** Автоматически генерируемый файл для фиксации версий зависимостей.
  - **Содержание:** Точные версии всех используемых библиотек. Гарантирует воспроизводимость сборки. **Не редактируйте этот файл вручную.**

- **`Makefile`**
  - **Назначение:** Упрощение рутинных задач через команду `make`.
  - **Содержание:** Команды для управления версиями (`version-status`, `version-bump-patch`), синхронизации конфига и других задач. Автоматически определяет ОС (Windows/macOS) для выбора правильного интерпретатора Python.

### 📄 Документация

- **`README.md`**
  - **Назначение:** Основная документация для Rust-версии.
  - **Содержание:** Инструкции по запуску, сборке и статус миграции функционала с Python.

- **`CHEAT_SHEET.md`**
  - **Назначение:** Шпаргалка для разработчика.
  - **Содержание:** Быстрые команды для сборки, тестирования и управления версиями.

- **`PROJECT_STRUCTURE.md`**
  - **Назначение:** Этот файл.
  - **Содержание:** Подробное описание структуры проекта.

---

## 📂 Исходный код (`rust/src/`)

Это папка с исходным кодом приложения.

### 🦀 `main.rs`
- **Назначение:** Точка входа в приложение и реализация GUI.
- **Ключевые компоненты:**
  - `fn main()`: Инициализация окна и запуск приложения через `eframe`.
  - `struct ApiAiApp`: Основная структура состояния приложения (хранит настройки, текст ввода, результаты).
  - `impl eframe::App for ApiAiApp`: Реализация логики отрисовки интерфейса (методы `update`, `ui`).
  - **Функционал:** Отрисовка главного окна, диалога настроек, обработка ввода пользователя, логика PIN-кода.

### 🦀 `api.rs`
- **Назначение:** Клиент для работы с внешними API.
- **Ключевые компоненты:**
  - `struct ApiClient`: Структура для выполнения HTTP запросов.
  - **Функционал:** Отправка запросов к Telegram Bot API (и в будущем к OpenAI/Anthropic), обработка ответов, шифрование данных (AES-GCM) перед отправкой.

---

## 📂 Артефакты сборки (`rust/target/`)

- **Назначение:** Папка для скомпилированных файлов.
- **Содержание:**
  - `debug/`: Сборка для разработки (быстрая компиляция, медленная работа).
  - `release/`: Оптимизированная сборка (медленная компиляция, быстрая работа).
- **Примечание:** Эта папка обычно весит много (сотни МБ) и находится в `.gitignore`.

---

## 🔗 Внешние связи

Проект взаимодействует с файлами вне папки `rust/`:

- **`../config_qt.json`**
  - Единый конфигурационный файл проекта (общий с Python версией).
  - Содержит API ключи, настройки темы и PIN-код.

- **`../python/scripts/update_version.py`**
  - Скрипт, вызываемый из `Makefile` для управления версиями.
