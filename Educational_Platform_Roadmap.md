# 🚀 Roadmap: ApiAi как Образовательная Платформа и База Знаний

Этот план превращает **ApiAi** из простого чат-клиента в мощный инструмент для обучения (Learning Management System - LMS) и персональную базу знаний (Second Brain), построенную на самых современных технологиях.

---

## 🏗 Фаза 1: Переход на "Bleeding Edge" Стек (Месяц 1-2)
**Цель:** Сделать кодовую базу эталоном современной разработки. Чтобы изучать технологии, нужно видеть их правильное применение.

### 1.1. Frontend: React 19 + TypeScript + Tailwind v4
*   **Зачем:** Vanilla JS хорош для старта, но индустрия требует React.
*   **Что сделать:**
    *   Миграция на **Vite** + **React 19** (изучаем новые хуки `use`, Server Actions концепции).
    *   **TypeScript** в строгом режиме (`strict: true`). Полная типизация всего, что движется.
    *   **Tailwind CSS v4** (alpha/beta) или v3.4 — для стилизации.
    *   **Zustand** или **Jotai** для стейт-менеджмента (вместо Redux, чтобы показать современные подходы).
    *   **Shadcn/ui** — использование headless компонентов для создания профессионального UI.

### 1.2. Backend: Rust Async & Architecture
*   **Зачем:** Показать "взрослый" Rust.
*   **Что сделать:**
    *   Внедрение **Clean Architecture** (разделение на слои: Domain, Data, API).
    *   Использование **Tokio** на полную мощность (каналы, мьютексы, спавн задач).
    *   **Error Handling**: переход на `thiserror` для библиотек и `anyhow` для приложения.
    *   **Tracing**: внедрение структурного логирования (вместо `println!`).

---

## 🧠 Фаза 2: Knowledge Engine (База Знаний) (Месяц 3-4)
**Цель:** Превратить приложение в "Второй Мозг", который помогает учиться.

### 2.1. RAG (Retrieval-Augmented Generation)
*   **Технологии:** `Qdrant` (локально в Docker или embedded) или `LanceDB` (embedded Rust vector DB).
*   **Функционал:**
    *   Пользователь кидает PDF/MD файлы (книги, статьи) в папку.
    *   Rust автоматически индексирует их (разбивает на чанки, генерирует эмбеддинги).
    *   Чат может отвечать на вопросы *по вашим материалам*.
    *   **Учебный момент:** Изучение векторов, эмбеддингов, NLP.

### 2.2. Smart Notes & Links
*   **Технологии:** Markdown парсеры, Graph visualization.
*   **Функционал:**
    *   Поддержка `[[WikiLinks]]` для связи заметок.
    *   Визуализация графа знаний (как в Obsidian).
    *   Автоматическая генерация тегов и связей через AI.

---

## 🤖 Фаза 3: Local AI & Agentic Workflow (Месяц 5-6)
**Цель:** Независимость от облаков и создание автономных агентов.

### 3.1. Локальные LLM (Ollama Integration)
*   **Технологии:** Ollama API, Rust bindings.
*   **Функционал:**
    *   Полная работа без интернета.
    *   Переключение моделей на лету (Llama 3, Mistral, Gemma).
    *   **Privacy-first**: ваши данные не покидают компьютер.

### 3.2. AI Агенты
*   **Технологии:** LangChain (Rust port) или кастомная реализация.
*   **Функционал:**
    *   Агент "Рисечер": Даешь тему -> он гуглит (через API), читает 5 статей, пишет саммари.
    *   Агент "Кодер": Пишет код, запускает тесты (в песочнице), исправляет ошибки.
    *   **Учебный момент:** Понимание паттернов ReAct, Chain-of-Thought.

---

## 🛠 Фаза 4: Engineering Excellence (Параллельно)
**Цель:** Изучение процессов разработки, а не только кодинга.

### 4.1. CI/CD & DevOps
*   **GitHub Actions:** Автоматическая сборка релизов для Mac/Windows/Linux.
*   **Testing:**
    *   Unit tests в Rust.
    *   Component tests в React (Vitest + Testing Library).
    *   E2E tests (Playwright) — тестирование всего приложения.

### 4.2. Плагинна архитектура
*   **Цель:** Позволить другим писать плагины для вашего приложения.
*   **Реализация:** WebAssembly (Wasm) плагины или динамическая загрузка JS модулей.

---

## 🎓 Как использовать это для обучения?

1.  **Режим "Code Tour":** В приложении сделать режим, где при наведении на элемент UI показывается всплывающее окно с объяснением: "Этот компонент написан на React, он вызывает Rust функцию X через Tauri Command Y".
2.  **Интерактивные туториалы:** Встроенные в приложение уроки, которые проверяют, выполнили ли вы задание (например, "Создайте файл test.md").
3.  **Open Source:** Вести разработку открыто, с качественными коммитами и Pull Request'ами, чтобы другие могли учиться на истории изменений.
