# 🚀 Настройка проекта ApiAi

Инструкция по настройке проекта **ApiAi** после клонирования с GitHub.

---

## 📋 Что происходит при клонировании

При клонировании репозитория с GitHub вы получаете:

✅ **Есть в репозитории:**
- Исходный код (Python модули)
- Template файл конфигурации: `config/config_qt.json.template`
- Шрифты DejaVu Sans в `fonts/`
- Документация в `docs/`
- Скрипты в `scripts/`
- Скрипты сборки инсталляторов в `deployment/`

❌ **НЕТ в репозитории** (они в `.gitignore`):
- `config_qt.json` - локальный конфиг с вашими API ключами и настройками
- `venv/` или `.venv/` - виртуальное окружение Python
- `offline_packages/*.whl` - офлайн пакеты для установки

---

## 💿 Вариант 1: Установка через инсталлятор (рекомендуется)

Если вы **конечный пользователь** и просто хотите использовать приложение:

### Windows

1. Скачайте `ApiAi-Setup-X.X.X.exe` с [GitHub Releases](https://github.com/kureinmaxim/ApiAi/releases)
2. Запустите установщик
3. Следуйте инструкциям мастера установки
4. Запускайте через ярлык в Start Menu

### macOS

1. Скачайте `ApiAi-X.X.X-macOS.dmg` с [GitHub Releases](https://github.com/kureinmaxim/ApiAi/releases)
2. Откройте DMG файл
3. Перетащите `ApiAi.app` в папку `Applications`
4. При первом запуске разрешите запуск в System Preferences → Security

---

## 🔧 Вариант 2: Установка из исходников (для разработчиков)

Если вы **разработчик** и хотите модифицировать код:

### Шаг 1: Клонируйте репозиторий

```bash
git clone <repo-url> ApiAi
cd ApiAi
```

### Шаг 2: Создайте виртуальное окружение

**Windows:**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate
```

**macOS/Linux:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3: Установите зависимости

```bash
pip install -r requirements.txt
```

**Минимальные зависимости (всегда):**
- PySide6 (GUI)
- requests (HTTP)
- cryptography (шифрование)

**Опциональные (для экспорта в Word/PDF):**
- python-docx
- reportlab

### Шаг 4: Запустите приложение

```bash
python main.py
```

При первом запуске приложение автоматически создаст `config_qt.json` из шаблона.

---

## 🛠️ Ручная настройка config

Если хотите создать config вручную:

**Windows PowerShell:**
```powershell
Copy-Item config/config_qt.json.template config_qt.json
```

**macOS/Linux (bash):**
```bash
cp config/config_qt.json.template config_qt.json
```

---

## 📂 Структура config файла

### `config_qt.json`

```json
{
  "app_info": {
    "version": "1.0.0",
    "release_date": "30.11.2025",
    "developer_en": "Kurein M.N."
  },
  "security": {
    "pin_code": "1234",
    "require_pin": true
  },
  "window": {
    "width": 900,
    "height": 700,
    "scale_factor": 1.0
  },
  "ui": {
    "theme": "dark",
    "font_size": 12,
    "scale_factor": 1.0
  },
  "api_keys": {
    "anthropic": "",
    "openai": "",
    "telegram_url": "http://localhost:8000/ai_query",
    "telegram_key": "",
    "telegram_enc_key": "",
    "telegram_use_encryption": true,
    "app_id": "apiai-v1"
  }
}
```

**Важно:** Файл `config_qt.json` находится в `.gitignore` чтобы сохранить ваши API ключи и персональные настройки (PIN, size, theme).

---

## 🔄 Синхронизация версий

После обновления проекта (`git pull`) синхронизируйте версии:

```bash
python scripts/update_version.py sync
```

**Что делает sync:**
- ✅ Обновляет версию в локальном `config_qt.json`
- ✅ Синхронизирует `app_info` из template
- ⚠️ **НЕ затрагивает** ваши персональные настройки (theme, scale_factor, API keys)

---

## 🤖 Настройка AI интеграции

ApiAi поддерживает три способа работы с AI:

### Способ 1: Через Telegram Bot (рекомендуется)

Не требует собственных API ключей!

1. **Получите доступ:**
   - Отправьте команду `/api` боту в Telegram (только для админа)
   - Скопируйте URL и API Key

2. **Настройте в ApiAi:**
   - Откройте ApiAi
   - Двойной клик на имени разработчика (футер)
   - Введите PIN (default: `1234`)
   - Меню → PDF Search → Settings
   - Вкладка API Keys → Telegram Bot API
   - Вставьте URL и Key
   - Нажмите "Проверить соединение"

### Способ 2: Напрямую через Anthropic/OpenAI

Если у вас есть собственные API ключи:

1. Откройте Settings → API Keys
2. Введите ключи:
   - **Anthropic**: `sk-ant-api03-...`
   - **OpenAI**: `sk-proj-...`

**Где получить ключи:**
- Anthropic: [console.anthropic.com](https://console.anthropic.com/)
- OpenAI: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

---

## 🧪 Проверка установки

### 1. Проверьте что config создан:

```bash
# Windows PowerShell
Test-Path config_qt.json

# macOS/Linux
ls -la config_qt.json
```

### 2. Проверьте версию:

```bash
python scripts/update_version.py status
```

### 3. Запустите приложение:

```bash
python main.py
```

---

## ❓ FAQ

### Q: Почему нет `config_qt.json` в Git?

**A:** Этот файл содержит ваши API ключи и персональные настройки (PIN, theme, размер окна). Если бы он был в Git, при каждом `git pull` ваши настройки перезаписывались бы.

### Q: Что делать после `git pull`?

**A:** Запустите `python scripts/update_version.py sync` для синхронизации версии. Скрипт обновит только `app_info`, сохранив API ключи и настройки.

### Q: Можно ли вручную изменить config?

**A:** Да! `config_qt.json` — это обычный JSON файл. Редактируйте в любом текстовом редакторе.

### Q: Как обновить структуру config при добавлении новых полей?

**A:** Вручную добавьте новые поля из `config/config_qt.json.template` в ваш `config_qt.json`. Или удалите локальный config — приложение создаст новый из template.

### Q: Нужно ли устанавливать reportlab и python-docx?

**A:** Опционально. Они нужны только для экспорта результатов в PDF/Word. Если планируете использовать эти форматы:
```bash
pip install reportlab python-docx
```

---

## 📚 Дополнительная информация

| Документ | Описание |
|----------|----------|
| `README.md` | Основная информация о проекте |
| `docs/VERSION_MANAGEMENT.md` | Управление версиями |
| `docs/AI_INTEGRATION_GUIDE.md` | Настройка AI интеграции |
| `docs/API_MANAGEMENT.md` | Управление API ключами |
| `docs/OFFLINE_INSTALLATION_GUIDE.md` | Офлайн установка |
| `docs/DISPLAY_FIXES.md` | Проблемы отображения |

### Полезные команды

```bash
# Проверить версию
python scripts/update_version.py status

# Синхронизировать версию
python scripts/update_version.py sync

# Скачать шрифты (если удалили fonts/)
python scripts/download_fonts.py

# Запустить приложение
python main.py
```

---

**Дата создания:** 30.11.2025  
**Автор:** Kurein M.N.  
**Версия документа:** 1.0
