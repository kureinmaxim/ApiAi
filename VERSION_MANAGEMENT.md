# 📦 Version Management Guide

Руководство по управлению версиями ApiAi и синхронизации между всеми компонентами проекта.

---

## 🎯 Общий принцип

ApiAi использует **единую версию** для всех компонентов:
- **shared-rs** - общая библиотека (NEW!)
- Tauri приложение
- Python приложение  
- Rust CLI
- Конфигурационные файлы

Версия хранится в `config_qt.json` и автоматически синхронизируется во все необходимые файлы.

> **Текущая версия:** `2.1.1` (синхронизировано 03.12.2025)

---

## 🔢 Формат версии

Используется **Semantic Versioning** (SemVer): `MAJOR.MINOR.PATCH`

```
1.0.5
│ │ │
│ │ └─ PATCH: исправления багов, мелкие правки
│ └─── MINOR: новые функции, обратно совместимые
└───── MAJOR: значительные изменения, API breaking changes
```

**Примеры:**
- `1.0.5 → 1.0.6` - patch (исправление бага)
- `1.0.5 → 1.1.0` - minor (новая функция)
- `1.0.5 → 2.0.0` - major (крупное обновление)

---

## 🛠️ Команды управления версиями

Все команды выполняются из папки `rust/`:

```bash
cd rust
```

### Проверить текущую версию

```bash
make version-status
```

**Вывод:**
```
Current version: 2.1.1
Files to sync:
  ✓ config_qt.json
  ✓ python/config/config_qt.json.template
  ✓ shared-rs/Cargo.toml
  ✓ rust/Cargo.toml
  ✓ tauri-app/src-tauri/Cargo.toml
```

### Синхронизировать конфигурацию

```bash
make version-sync
```

Обновляет все файлы конфигурации из `config_qt.json` без изменения версии.

### Увеличить версию

**Patch (2.1.1 → 2.1.2):**
```bash
make version-bump-patch
```

**Minor (2.1.1 → 2.2.0):**
```bash
make version-bump-minor
```

**Major (2.1.1 → 3.0.0):**
```bash
make version-bump-major
```

### Установить конкретную версию

```bash
make version-set v=X.Y.Z
```

**Пример:**
```bash
make version-set v=2.1.2
```

---

## 📁 Файлы, которые обновляются

При изменении версии автоматически обновляются:

### 1. **config_qt.json** (корень)
```json
{
  "app_info": {
    "version": "2.1.1"
  }
}
```

### 2. **python/config/config_qt.json.template**
```json
{
  "app_info": {
    "version": "2.1.1"
  }
}
```

### 3. **shared-rs/Cargo.toml** (NEW!)
```toml
[package]
name = "apiai-shared"
version = "2.1.1"
```

### 4. **rust/Cargo.toml**
```toml
[package]
version = "2.1.1"
```

### 5. **tauri-app/src-tauri/Cargo.toml**
```toml
[package]
version = "2.1.1"
```

### 6. **tauri-app/src-tauri/tauri.conf.json**
```json
{
  "version": "2.1.1"
}
```

### 7. **tauri-app/src/index.html**
```html
<span class="footer-value">2.1.1</span>
```

---

## 🔄 Процесс синхронизации

### Схема работы

```
config_qt.json (источник истины)
     │
     ├─→ python/config/config_qt.json.template
     ├─→ shared-rs/Cargo.toml (NEW!)
     ├─→ rust/Cargo.toml
     └─→ tauri-app/src-tauri/Cargo.toml
         ├─→ tauri-app/src-tauri/tauri.conf.json
         └─→ tauri-app/src/index.html
```

### Как это работает

1. **Версия в config_qt.json** - главный источник
2. **Скрипт update_version.py** читает и обновляет все файлы
3. **Makefile** вызывает Python скрипт с нужными параметрами
4. Все изменения применяются атомарно

---

## 🚀 Типичные сценарии

### Исправление бага

```bash
cd rust

# 1. Проверить текущую версию
make version-status

# 2. Увеличить patch версию
make version-bump-patch
# 2.1.1 → 2.1.2

# 3. Проверить изменения
git diff config_qt.json shared-rs/Cargo.toml rust/Cargo.toml
```

### Новая функция

```bash
cd rust

# Увеличить minor версию
make version-bump-minor
# 2.1.1 → 2.2.0
```

### Крупное обновление

```bash
cd rust

# Увеличить major версию
make version-bump-major
# 2.1.1 → 3.0.0
```

### Откат на конкретную версию

```bash
cd rust

# Установить нужную версию
make version-set v=2.1.0
```

### Ручное редактирование config_qt.json

Если вы вручную изменили `config_qt.json`:

```bash
cd rust

# Синхронизировать все файлы
make version-sync
```

---

## 🔍 Проверка согласованности

### Проверить, что все файлы синхронизированы

```bash
cd rust
make version-status
```

Если версии различаются, запустите:
```bash
make version-sync
```

### Проверить версию в каждом файле вручную

```bash
# config_qt.json
jq '.app_info.version' ../config_qt.json

# Rust
grep '^version' Cargo.toml

# Tauri
grep '"version"' ../tauri-app/src-tauri/tauri.conf.json

# Python template
jq '.app_info.version' ../python/config/config_qt.json.template
```

---

## ⚙️ Настройка окружения

### Требования

Для работы системы управления версиями нужно:
- Python 3.6+ (для скрипта update_version.py)
- make (обычно предустановлен на macOS/Linux)

### Проверка

```bash
# Python
python3 --version

# Make
make --version
```

### Windows

На Windows `make` может отсутствовать. Альтернативы:

**1. Использовать Python напрямую:**
```bash
cd python
python scripts/update_version.py status
python scripts/update_version.py bump --type patch
```

**2. Установить make через Chocolatey:**
```bash
choco install make
```

**3. Использовать WSL:**
```bash
wsl
cd /mnt/c/path/to/ApiAi/rust
make version-status
```

---

## 🎨 Дополнительно в Tauri

### Обновление версии в footer

После изменения версии в Tauri приложении нужно обновить `index.html`:

```html
<!-- tauri-app/src/index.html -->
<div class="footer-item">
  <span class="footer-label">Version:</span>
  <span class="footer-value">1.0.5</span> <!-- Обновить здесь -->
</div>
```

**Автоматизация:** Можно сделать динамическую загрузку версии из config в будущем.

---

## 📝 Best Practices

### ✅ DO

- Всегда используйте `make` команды для изменения версий
- Проверяйте `make version-status` перед коммитом
- Обновляйте версию перед релизом
- Используйте семантическое версионирование
- Коммитьте изменения версии отдельным коммитом

### ❌ DON'T

- Не редактируйте версии вручную в разных файлах
- Не забывайте синхронизировать после ручных правок
- Не пропускайте версии (1.0.5 → 1.0.7)
- Не используйте произвольные форматы версий

---

## 🔧 Troubleshooting

### Версии не синхронизируются

**Проблема:** После `make version-bump-patch` версия изменилась не везде

**Решение:**
```bash
cd rust
make version-sync
```

### Скрипт не найден

**Проблема:** `python: command not found`

**Решение (macOS/Linux):**
```bash
# Попробовать python3
python3 --version

# Или установить Python
brew install python3  # macOS
```

**Решение (Windows):**
```bash
# Скачать с python.org и установить
# Или использовать py launcher
py --version
```

### Make не работает на Windows

**Решение:** Используйте Python скрипт напрямую:
```bash
cd python
python scripts/update_version.py bump --type patch
```

### Конфликт версий в Git

**Проблема:** Merge конфликт в `config_qt.json`

**Решение:**
1. Разрешите конфликт вручную
2. Выберите правильную версию
3. Запустите `make version-sync`
4. Закоммитьте результат

---

## 📊 История версий

### Текущая версия: 2.1.1

**Changelog:**
- **2.1.1** (03.12.2025)
  - Создана общая библиотека `shared-rs`
  - Устранено дублирование кода (774 строки)
  - Синхронизирована версия Python (с 1.0.3 до 2.1.1)
  - Добавлены команды очистки (`make clean-all`)
  - Обновлен .gitignore для target/ папок
  
- **1.0.5** (2025-12-01)
  - Tauri UI модернизация
  - PIN защита настроек
  - Футер с информацией
  
- **1.0.4** (2024-11-30)
  - Улучшения UI
  
- **1.0.3** (2024-11-29)
  - Исправления багов

### Планируемые версии

- **2.1.2** - Мелкие исправления
- **2.2.0** - Новые функции
- **3.0.0** - Крупное обновление API

---

## 🎯 Быстрая справка

```bash
# Из папки rust/

make version-status         # Проверить версию
make version-sync           # Синхронизировать файлы
make version-bump-patch     # 2.1.1 → 2.1.2
make version-bump-minor     # 2.1.1 → 2.2.0
make version-bump-major     # 2.1.1 → 3.0.0
make version-set v=X.Y.Z    # Установить конкретную версию
```

---

## 📦 Компоненты проекта

Все компоненты на версии **2.1.1**:

- `shared-rs/Cargo.toml` → 2.1.1 (общая библиотека)
- `rust/Cargo.toml` → 2.1.1 (CLI версия)
- `tauri-app/src-tauri/Cargo.toml` → 2.1.1 (GUI версия)
- `python/config/config_qt.json.template` → 2.1.1 (Python версия)
- `config_qt.json` → 2.1.1 (главный конфиг)

---

## 👨‍💻 Разработчик

**Maksim Kurein**  
Текущая версия: **2.1.1**  
Дата релиза: **03.12.2025**
