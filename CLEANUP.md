# Очистка проекта от временных файлов

## 📊 Проблема

Проект занимает **11 GB**, из которых **8.4 GB** - это временные файлы сборки, которые можно безопасно удалить.

## 🔍 Что занимает место

| Папка | Размер | Можно удалить? |
|-------|--------|----------------|
| `rust/target/` | 3.0 GB | ✅ Да |
| `tauri-app/src-tauri/target/` | 5.4 GB | ✅ Да |
| `python/venv/` | 1.2 GB | ⚠️ Опционально |
| `venv/` (корневая) | 1.2 GB | ⚠️ Опционально |
| Исходный код | ~200 MB | ❌ Нет |

## ✅ Быстрая очистка (Рекомендуется)

### Удалить все build artifacts (~8.4 GB)

```bash
# Вариант 1: Использовать Makefile (рекомендуется)
make clean-all

# Вариант 2: Вручную через cargo
cd rust
cargo clean

cd ../tauri-app/src-tauri
cargo clean
```

**Результат:** Проект уменьшится с **11 GB → ~2.6 GB**

### Проверить размер

```bash
make size
```

## ⚠️ Опциональная очистка

### Удалить Python virtual environments (~2.4 GB)

```bash
make clean-python

# Или вручную:
rm -rf venv python/venv
```

**Результат:** Проект уменьшится до **~0.2 GB** (только исходники)

> **⚠️ Внимание:** После этого нужно будет пересоздать venv:
> ```bash
> cd python
> python3 -m venv venv
> source venv/bin/activate  # macOS/Linux
> # или .venv\Scripts\activate  # Windows
> pip install -r requirements.txt
> ```

## 📋 Доступные команды

```bash
# Очистка конкретных компонентов
make clean-rust      # Удалить rust/target (~3GB)
make clean-tauri     # Удалить tauri-app/src-tauri/target (~5.4GB)
make clean-python    # Удалить venv (~2.4GB)

# Полная очистка
make clean-all       # Удалить все build artifacts (~8.4GB)

# Проверка
make size           # Показать размер проекта
```

## ❓ Частые вопросы

### Безопасно ли удалять target/?
**Да.** Эти папки содержат только результаты компиляции. При следующем запуске `cargo build` или `npm run tauri dev` они будут созданы заново.

### Как долго пересоздаются файлы?
Первая сборка после очистки займет **3-5 минут**. Последующие сборки будут быстрее благодаря инкрементальной компиляции.

### Нужно ли это делать регулярно?
**Рекомендуется** очищать build artifacts раз в 1-2 недели или перед коммитом в Git, чтобы освобождать место на диске.

### Почему .gitignore не помогает?
`.gitignore` уже настроен и предотвращает попадание этих файлов в Git. Но локально они все равно занимают место на диске.

## 🎯 Рекомендуемая процедура

1. **Перед началом работы:**
   ```bash
   make size  # Проверить текущий размер
   ```

2. **Очистка:**
   ```bash
   make clean-all  # Освободить 8.4 GB
   ```

3. **Проверка:**
   ```bash
   make size  # Убедиться что размер ~2.6 GB
   ```

4. **Продолжить работу:**
   - Rust CLI: `cd rust && cargo run`
   - Tauri: `cd tauri-app && npm run tauri dev`

---

> **💡 Совет:** Добавьте `make clean-all` в свой workflow перед пушем в Git или перед переключением на другой проект.
