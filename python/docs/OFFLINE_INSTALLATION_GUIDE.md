# 📦 Офлайн установка ApiAi

Инструкция по установке приложения на компьютер **без доступа к интернету**.

---

## 📋 Содержание

1. [Краткий алгоритм](#-краткий-алгоритм)
2. [Подготовка на ПК с интернетом](#-подготовка-на-пк-с-интернетом)
3. [Установка на ПК без интернета](#-установка-на-пк-без-интернета)
4. [Решение проблем](#-решение-проблем)

---

## 🚀 Краткий алгоритм

| Шаг | Действие |
|-----|----------|
| 1 | **На ПК с интернетом:** Скачать исходный код + зависимости |
| 2 | **Перенос:** Скопировать файлы на флешку |
| 3 | **На ПК без интернета:** Установить Python + зависимости |
| 4 | **Запуск:** `python main.py` |

---

## 💻 Подготовка на ПК с интернетом

### Шаг 1: Скачать исходный код

```bash
# Клонировать репозиторий
git clone <repo-url> ApiAi

# Или скачать ZIP с GitHub
```

### Шаг 2: Скачать Python установщик

Для Windows:
- [python.org/downloads](https://www.python.org/downloads/) → Python 3.8+
- Выберите **Windows installer (64-bit)**

Для macOS:
- Python уже установлен или используйте Homebrew
- `brew install python3`

### Шаг 3: Подготовить offline_packages

```bash
# В папке ApiAi создайте директорию
mkdir offline_packages

# Скачайте все зависимости
pip download -r requirements.txt -d offline_packages

# Для Windows (если готовите на macOS/Linux):
pip download -r requirements.txt \
    -d offline_packages \
    --platform win_amd64 \
    --python-version 38 \
    --only-binary=:all:
```

**Что будет скачано:**

| Библиотека | Размер | Назначение |
|------------|--------|------------|
| **PySide6** | ~80 MB | GUI framework |
| **requests** | ~1 MB | HTTP client |
| **cryptography** | ~5 MB | Encryption |
| Зависимости | ~20 MB | Транзитивные зависимости |

📊 **Всего:** ~100-120 MB

### Шаг 4: Подготовить флешку

Скопируйте на флешку:
```
📁 USB Drive
├── 📁 ApiAi/              # Исходный код проекта
├── 📁 offline_packages/   # Скачанные .whl файлы
└── 📄 python-installer.exe  # Установщик Python (для Windows)
```

---

## 🎒 Установка на ПК без интернета

### Windows

```powershell
# 1. Скопируйте файлы с флешки (например, в C:\Apps\ApiAi)

# 2. Установите Python (если нужно)
# Запустите python-installer.exe
# ✅ Отметьте "Add Python to PATH"

# 3. Откройте PowerShell в папке проекта
cd C:\Apps\ApiAi

# 4. Создайте виртуальное окружение
python -m venv .venv

# 5. Активируйте его
.\.venv\Scripts\Activate

# 6. Установите зависимости из offline_packages
pip install --no-index --find-links=offline_packages -r requirements.txt

# 7. Запустите приложение
python main.py
```

### macOS / Linux

```bash
# 1. Скопируйте файлы с флешки
cp -r /Volumes/USB/ApiAi ~/Applications/

# 2. Откройте Terminal в папке проекта
cd ~/Applications/ApiAi

# 3. Создайте виртуальное окружение
python3 -m venv .venv

# 4. Активируйте его
source .venv/bin/activate

# 5. Установите зависимости
pip install --no-index --find-links=offline_packages -r requirements.txt

# 6. Запустите приложение
python3 main.py
```

---

## 🐛 Решение проблем

### ❌ "Python не найден"

**Windows:**
- Переустановите Python с галочкой "Add Python to PATH"
- Или используйте полный путь: `C:\Users\{Вы}\AppData\Local\Programs\Python\Python3XX\python.exe`

**macOS:**
- Используйте `python3` вместо `python`
- Или установите через Homebrew: `brew install python3`

### ❌ "No module named 'PySide6'"

**Причина:** Виртуальное окружение не активировано или зависимости не установлены

**Решение:**
```bash
# Активируйте окружение
# Windows:
.\.venv\Scripts\Activate

# macOS/Linux:
source .venv/bin/activate

# Переустановите зависимости
pip install --no-index --find-links=offline_packages -r requirements.txt
```

### ❌ "No matching distribution found"

**Причина:** Пакеты скачаны для другой платформы/версии Python

**Решение:**
1. Проверьте версию Python: `python --version`
2. Перескачайте пакеты с правильными параметрами:

```bash
# Для Python 3.8 Windows 64-bit
pip download -r requirements.txt \
    -d offline_packages \
    --platform win_amd64 \
    --python-version 38 \
    --only-binary=:all:

# Для Python 3.11 Windows 64-bit
pip download -r requirements.txt \
    -d offline_packages \
    --platform win_amd64 \
    --python-version 311 \
    --only-binary=:all:
```

### ❌ "ImportError: DLL load failed" (Windows)

**Причина:** Отсутствуют Microsoft Visual C++ Redistributables

**Решение:**
1. Скачайте на ПК с интернетом:  
   [Microsoft Visual C++ Redistributable](https://aka.ms/vs/17/release/vc_redist.x64.exe)
2. Скопируйте на флешку
3. Установите на целевом ПК

### ❌ Ошибки шифрования

**Причина:** Библиотека `cryptography` требует дополнительные системные библиотеки

**Windows:**
- Обычно работает из коробки

**macOS:**
- Может потребоваться: `brew install openssl`

**Linux:**
```bash
# Ubuntu/Debian
sudo apt-get install libssl-dev

# Fedora
sudo dnf install openssl-devel

# Arch
sudo pacman -S openssl
```

---

## ✅ Чек-лист для администратора

Для успешного развертывания нужны:

| Компонент | Windows | macOS | Linux |
|-----------|---------|-------|-------|
| Исходный код ApiAi | ✅ | ✅ | ✅ |
| Папка `offline_packages` | ✅ | ✅ | ✅ |
| Python 3.8+ installer | ✅ | ❌* | ❌* |
| Visual C++ Redistributable | ✅ | ❌ | ❌ |

*Обычно уже установлен в системе

---

## 📦 Минимальные требования

| Параметр | Требование |
|----------|------------|
| **ОС** | Windows 10+, macOS 10.15+, Linux (любой) |
| **Python** | 3.8 или новее |
| **RAM** | 512 MB свободной памяти |
| **Диск** | 500 MB свободного места |
| **Экран** | 1024x768 или выше |

---

## 📚 Связанная документация

- [README.md](../README.md) — Основная документация
- [API_MANAGEMENT.md](API_MANAGEMENT.md) — Настройка API ключей

---

**Последнее обновление:** 30.11.2025  
**Версия документа:** 1.0
