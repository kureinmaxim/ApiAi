# 🤖 Руководство по AI-интеграции ApiAi

> **Версия:** 1.0  
> **Дата:** 30.11.2025  
> **Автор:** Kurein M.N.

---

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Поддерживаемые AI провайдеры](#-поддерживаемые-ai-провайдеры)
3. [Настройка API ключей](#-настройка-api-ключей)
4. [Безопасность](#-безопасность)
5. [Шифрование](#-шифрование)
6. [Примеры использования](#-примеры-использования)
7. [Troubleshooting](#-troubleshooting)

---

## 🚀 Быстрый старт

### Настройка ApiAi

1. Запустите **ApiAi**
2. **Двойной клик** по имени разработчика в футере
3. Введите **PIN** (по умолчанию: `1234`)
4. Откройте меню **PDF Search** → **Settings (API Keys)**
5. Введите ключи для нужных провайдеров
6. Нажмите **OK**

### Проверка работы

1. В главном окне выберите провайдер (Anthropic Claude / OpenAI GPT / Telegram Bot)
2. Введите название компонента: `TPS54302`
3. Нажмите **🔍 Поиск**

Если всё настроено правильно — вы получите информацию о компоненте!

---

## 🤖 Поддерживаемые AI провайдеры

ApiAi поддерживает три способа взаимодействия с AI:

### 1. Anthropic Claude (прямой вызов)

```json
{
  "api_keys": {
    "anthropic": "sk-ant-api03-..."
  }
}
```

**Получение ключа:** [console.anthropic.com](https://console.anthropic.com/)

**Модель:** Claude 3.5 Sonnet  
**Рекомендуется для:** Точного анализа компонентов

### 2. OpenAI GPT (прямой вызов)

```json
{
  "api_keys": {
    "openai": "sk-proj-..."
  }
}
```

**Получение ключа:** [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

**Модель:** GPT-4o  
**Рекомендуется для:** Быстрых запросов

### 3. Telegram Bot API (через TelegramHelper)

```json
{
  "api_keys": {
    "telegram_url": "http://YOUR_SERVER_IP:8000/ai_query",
    "telegram_key": "754c7afb...",
    "telegram_enc_key": "ключ_шифрования_32_байта",
    "telegram_use_encryption": true,
    "app_id": "apiai-v1"
  }
}
```

**Получение ключа:** Команда `/api` в боте (только для админа)

**Рекомендуется для:** Централизованного управления API ключами

---

## 🔑 Настройка API ключей

### Через GUI (рекомендуется)

1. **Разблокировать режим эксперта:**
   - Двойной клик на имени разработчика в футере
   - Ввести PIN (default: `1234`)

2. **Открыть настройки:**
   - Меню → PDF Search → Settings

3. **Заполнить форму:**
   - **Anthropic Claude API Key** (опционально)
   - **OpenAI GPT API Key** (опционально)
   - **Telegram Bot API** (рекомендуется):
     - URL эндпоинта
     - API ключ
     - Encryption Key (для безопасности)
     - Включить/выключить шифрование

4. **Протестировать соединение:**
   - Кнопка "🔄 Проверить соединение" (для Telegram Bot)

### Через конфигурационный файл

Путь к файлу: `ApiAi/config_qt.json`

```json
{
  "api_keys": {
    "anthropic": "sk-ant-...",
    "openai": "sk-...",
    "telegram_url": "http://IP:8000/ai_query",
    "telegram_key": "your_key",
    "telegram_enc_key": "encryption_32bytes_hex",
    "telegram_use_encryption": true,
    "app_id": "apiai-v1"
  }
}
```

---

## 🛡 Безопасность

### Многоуровневая защита API

ApiAi использует несколько уровней безопасности при работе с Telegram Bot API:

#### 1. HTTPS/TLS Шифрование
Все запросы передаются через защищенный канал (если сервер поддерживает HTTPS).

#### 2. API Key Аутентификация
```python
headers = {
    "X-API-KEY": "your_secret_key",
    "X-APP-ID": "apiai-v1"
}
```

#### 3. HMAC Подпись запросов
```python
# Формирование подписи
timestamp = int(time.time())
nonce = str(uuid.uuid4())
signature = hmac_sha256(payload + timestamp + nonce, secret)

headers.update({
    "XTimestamp": timestamp,
    "X-Nonce": nonce,
    "X-Signature": signature
})
```

#### 4. APP_ID Whitelist
Сервер проверяет, что `app_id` находится в списке разрешенных приложений.

### Генерация безопасных ключей

```bash
# API Secret Key (64 символа)
python3 -c "import secrets; print(secrets.token_hex(32))"

# Encryption Key (32 байта hex)
openssl rand -hex 32
```

---

## 🔐 Шифрование

ApiAi поддерживает **опциональное end-to-end шифрование** при работе с Telegram Bot API.

### Как это работает

```
┌─────────────────┐                    ┌─────────────────┐
│     ApiAi       │                    │  TelegramHelper │
│                 │                    │                 │
│  1. Payload     │                    │                 │
│  2. Encrypt     │  ──── Encrypted ──→│  3. Decrypt     │
│     (AES-256)   │        Data        │     (AES-256)   │
│  3. Base64      │                    │  4. Process     │
│                 │  ←─── Encrypted ──│  5. Encrypt     │
│  6. Decrypt     │      Response      │     Response    │
└─────────────────┘                    └─────────────────┘
```

### Алгоритмы

ApiAi использует `SecureMessenger` класс с двумя режимами:

1. **AES-256-GCM** (основной) - Authenticated Encryption
2. **Fernet** (fallback) - Symmetric encryption

### Настройка шифрования

#### В GUI:
1. Откройте Settings → API Keys
2. Раздел "Telegram Bot API"
3. Включите **"Шифрование"**
4. Введите **Encryption Key** (32 байта hex)

#### В config:
```json
{
  "api_keys": {
    "telegram_use_encryption": true,
    "telegram_enc_key": "a1b2c3...64_chars_hex"
  }
}
```

### Когда использовать шифрование

✅ **Используйте, если:**
- Сервер не поддерживает HTTPS
- Нужна дополнительная защита данных
- Передаются конфиденциальные данные

❌ **Можно не использовать, если:**
- Сервер использует HTTPS с валидным сертификатом
- Данные не конфиденциальны
- Нужна максимальная производительность

---

## 📝 Примеры использования

### Пример 1: Простой поиск компонента

```
Провайдер: Anthropic Claude
Запрос: TPS54302
Промпт: Стандартный (информация о компоненте)
```

**Результат:**
```
Полное название: TI TPS54302PWP
Производитель: Texas Instruments
Тип: DC-DC преобразователь (понижающий)
...
```

### Пример 2: Пользовательский промпт

```
Провайдер: OpenAI GPT
Запрос: ESP32-C3
Промпт: Пользовательский

Текст промпта:
"Расскажи про {query} для IoT проектов. Укажи основные характеристики,
примеры применения и аналоги."
```

### Пример 3: Через Telegram Bot с шифрованием

```
Провайдер: Telegram Bot
Запрос: STM32F103C8T6
Шифрование: Включено
```

ApiAi автоматически:
1. Зашифрует запрос (AES-256-GCM)
2. Отправит на сервер
3. Получит зашифрованный ответ
4. Расшифрует и отобразит результат

---

## 🆘 Troubleshooting

### ❌ "Не задан API ключ"

**Причина:** Ключ не заполнен в настройках

**Решение:**
1. Откройте Settings → API Keys
2. Заполните соответствующий ключ
3. Сохраните (OK)

### ❌ "Connection refused" (Telegram Bot)

**Причина:** Сервер недоступен

**Решение:**
1. Проверьте URL (должен быть `http://IP:PORT/ai_query`)
2. Проверьте доступность: `ping YOUR_SERVER_IP`
3. Проверьте firewall на сервере

### ❌ "401 Unauthorized" (Telegram Bot)

**Причина:** Неверный API ключ

**Решение:**
1. Получите актуальный ключ командой `/api` в боте
2. Обновите `telegram_key` в настройках

### ❌ "Ошибка расшифровки ответа"

**Причина:** Неверный ключ шифрования или сервер не поддерживает шифрование

**Решение:**
1. Проверьте `telegram_enc_key` в настройках
2. Или выключите шифрование: `telegram_use_encryption: false`

### ❌ "Модуль шифрования недоступен"

**Причина:** Не установлена библиотека `cryptography`

**Решение:**
```bash
pip install cryptography
```

---

## 🔧 Компоненты системы

### ApiAi (Desktop Client)

```python
# gui/search_widget.py
class SearchWidget(QWidget):
    """Виджет для взаимодействия с AI API"""
    
    def start_search(self):
        """Запускает поиск в отдельном потоке"""
        
    def _on_search_finished(self, results):
        """Обработка результатов"""

# gui/pdf_search.py
class AIPDFSearcher:
    """Класс для AI-поиска информации о компонентах"""
    
    def search(self, prompt: str) -> dict:
        """Выполняет поиск через выбранного провайдера"""
        
    def _search_anthropic(self, ...) -> dict:
        """Прямой вызов Anthropic API"""
        
    def _search_openai(self, ...) -> dict:
        """Прямой вызов OpenAI API"""
        
    def _search_telegram_bot(self, ...) -> dict:
        """Вызов через TelegramHelper API"""

# encryption.py
class SecureMessenger:
    """Класс для шифрования/расшифровки данных (AES-256-GCM/Fernet)"""
```

### Зависимости

```
PySide6>=6.5.0      # GUI framework
requests>=2.31.0    # HTTP client
cryptography>=41.0.0 # Encryption
```

---

## 📚 Связанная документация

- [API_MANAGEMENT.md](API_MANAGEMENT.md) — Управление API ключами
- [VERSION_MANAGEMENT.md](VERSION_MANAGEMENT.md) — Управление версиями
- [README.md](../README.md) — Основная документация

---

**Версия документа:** 1.0  
**Автор:** Kurein M.N.  
**Последнее обновление:** 30.11.2025
