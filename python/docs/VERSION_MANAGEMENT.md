# Управление версиями проекта ApiAi

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Схема версионирования](#схема-версионирования)
3. [Источник правды](#источник-правды)
4. [Инструмент update_version.py](#инструмент-update_versionpy)
5. [Рабочие сценарии](#рабочие-сценарии)
6. [FAQ](#faq)

---

## Обзор системы

В проекте ApiAi версии управляются **централизованно** через шаблон конфигурационного файла:

✅ **Единый источник правды** — версия хранится только в шаблоне  
✅ **Автоматическая синхронизация** — локальный config обновляется из шаблона  
✅ **Защита локальных настроек** — рабочий config файл в `.gitignore`  
✅ **Кроссплатформенность** — работает на Windows, macOS, Linux

---

## Схема версионирования

Используется [Semantic Versioning](https://semver.org/) в формате **MAJOR.MINOR.PATCH**:

| Часть | Когда увеличивать | Пример |
|-------|-------------------|--------|
| **MAJOR** | Критические изменения | `1.5.2` → `2.0.0` |
| **MINOR** | Новые функции (обратная совместимость) | `1.0.0` → `1.1.0` |
| **PATCH** | Исправление ошибок | `1.1.0` → `1.1.1` |

**Текущая версия:** v1.0.0

---

## Источник правды

### Где хранится версия

```
config/config_qt.json.template   → ИСТОЧНИК ПРАВДЫ (в Git)
```

Этот файл:
- ✅ Хранится в Git
- ✅ Содержит версию, дату релиза, имя разработчика
- ✅ Используется для синхронизации локального config

### Ключевые поля (секция `app_info`)

```json
{
  "app_info": {
    "version": "1.0.0",
    "release_date": "30.11.2025",
    "last_updated": "2025-11-30",
    "developer_en": "Kurein M.N."
  }
}
```

### Что НЕ является источником правды

❌ `config_qt.json` — локальный файл (не в Git, игнорируется)

---

## Инструмент update_version.py

Скрипт для управления версиями проекта.

### Расположение
```
scripts/update_version.py
```

### Основные команды

> Все команды работают одинаково на Windows, macOS и Linux.

#### 📊 Показать текущую версию
```bash
python scripts/update_version.py status
```

#### 🐛 Исправление бага (Patch)
```bash
python scripts/update_version.py bump --type patch
# 1.0.0 → 1.0.1
```

#### ✨ Новая функция (Minor)
```bash
python scripts/update_version.py bump --type minor
# 1.0.0 → 1.1.0
```

#### 💥 Критические изменения (Major)
```bash
python scripts/update_version.py bump --type major
# 1.0.0 → 2.0.0
```

#### 🎯 Установка конкретной версии
```bash
python scripts/update_version.py set 1.2.0
```

#### 🔄 Синхронизация локального config
```bash
python scripts/update_version.py sync
```

### Дополнительные опции

```bash
# Без обновления даты релиза
python scripts/update_version.py bump --type patch --no-release-date

# С конкретной датой релиза
python scripts/update_version.py set 1.5.0 --release-date 31.12.2025

# Изменить разработчика
python scripts/update_version.py set 1.0.0 --developer "Иванов И.И."

# Тестовый запуск (без записи)
python scripts/update_version.py bump --type patch --dry-run
```

### Устаревший формат (совместимость с BOMCategorizer)

```bash
# Также поддерживается:
python scripts/update_version.py --bump patch
python scripts/update_version.py --version 1.2.0
```

python scripts/update_version.py --version 1.2.0
```

---

## Управление из Rust проекта

Если вы находитесь в терминале в папке `rust/`, вы можете использовать простые команды `make`:

### 📊 Статус
```bash
make version-status
```

### 🆙 Обновление версии
```bash
make version-bump-patch   # 1.0.0 → 1.0.1
make version-bump-minor   # 1.0.0 → 1.1.0
make version-bump-major   # 1.0.0 → 2.0.0
```

### 🎯 Установка конкретной версии
```bash
make version-set v=1.2.3
```

---

## Рабочие сценарии

### 🎯 Сценарий 1: Релиз новой версии

```bash
# 1. Обновить версию
python scripts/update_version.py bump --type minor

# 2. Синхронизировать локальный config
python scripts/update_version.py sync

# 3. Проверить
python scripts/update_version.py status

# 4. Закоммитить
git add config/
git commit -m "Release: v1.1.0"

# 5. Создать тег (опционально)
git tag v1.1.0
git push origin v1.1.0
```

### 🎯 Сценарий 2: Работа на новой машине

```bash
git clone <repo-url>
cd ApiAi
python scripts/update_version.py status
python scripts/update_version.py sync
python main.py
```

### 🎯 Сценарий 3: После git pull

```bash
git pull
python scripts/update_version.py sync
```

---

## FAQ

### ❓ Как узнать текущую версию?
```bash
python scripts/update_version.py status
```

### ❓ Версии рассинхронизировались, что делать?
```bash
python scripts/update_version.py sync
```

### ❓ Команда sync затронет мои настройки?

**Нет!** Синхронизируется только секция `app_info` (версия, дата релиза, разработчик).

Ваши личные настройки (`scale_factor`, размер окна, тема, API ключи и т.д.) останутся без изменений.

### ❓ Почему config_qt.json в .gitignore?

Чтобы локальные настройки пользователя не попадали в Git. Каждая машина может иметь свои настройки, но версия проекта одна для всех.

### ❓ Как обновить только дату релиза без изменения версии?

```bash
python scripts/update_version.py set <current_version> --release-date 31.12.2025
```

---

## Файловая структура

```
ApiAi/
├── config/
│   └── config_qt.json.template  ← ИСТОЧНИК ПРАВДЫ
│
├── config_qt.json               (локальный, не в Git)
│
└── scripts/
    └── update_version.py        ← Инструмент управления версиями
```

---

**Последнее обновление:** 30.11.2025  
**Автор:** Kurein M.N.
