# Core dependencies
PySide6>=6.5.0
requests>=2.31.0
cryptography>=41.0.0

# AI providers (optional - install only if needed)
# anthropic>=0.34.0
# openai>=1.0.0

# Document generation (optional - for saving results)
python-docx>=1.1.0
reportlab>=4.0.0

# Build tools (optional - only for building installers)
 py2app>=0.28.0        # macOS .app bundle creation
 setuptools>=68.0.0    # Required for py2app
