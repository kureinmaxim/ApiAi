# 🔐 Управление API для ApiAi

> **Версия:** 1.0  
> **Дата:** 30.11.2025  
> **Автор:** Kurein M.N.

---

## 📋 Содержание

1. [Обзор](#-обзор)
2. [Быстрый старт](#-быстрый-старт)
3. [Конфигурация](#-конфигурация)
4. [Провайдеры API](#-провайдеры-api)
5. [Telegram Bot API](#-telegram-bot-api)
6. [Шифрование](#-шифрование)
7. [Тестирование](#-тестирование)
8. [Устранение проблем](#-устранение-проблем)

---

## 🎯 Обзор

ApiAi поддерживает три способа взаимодействия с AI:

| Провайдер | Описание | Требования |
|-----------|----------|------------|
| **Anthropic Claude** | Прямой вызов API | API ключ от Anthropic |
| **OpenAI GPT** | Прямой вызов API | API ключ от OpenAI |
| **Telegram Bot** | Через TelegramHelper сервер | URL + API ключ |

### Архитектура

```
┌─────────────────────┐                    ┌─────────────────────┐
│      ApiAi          │    HTTP Request    │   TelegramHelper    │
│   (Desktop App)     │ ────────────────→  │   (VPS Server)      │
│                     │                    │                     │
│  config_qt.json:    │    AI Response     │  .env:              │
│  - anthropic        │ ←────────────────  │  - API_SECRET_KEY   │
│  - openai           │                    │  - ANTHROPIC_KEY    │
│  - telegram_url     │                    │  - OPENAI_KEY       │
│  - telegram_key     │                    │                     │
└─────────────────────┘                    └─────────────────────┘
```

---

## 🚀 Быстрый старт

### Вариант 1: Прямое использование (Anthropic/OpenAI)

1. Откройте **ApiAi**
2. Разблокируйте (двойной клик на разработчике + PIN)
3. **Settings → API Keys**
4. Введите ключ Anthropic или OpenAI
5. **OK** → готово!

### Вариант 2: Через Telegram Bot

1. Получите URL и ключ от администратора бота (команда `/api`)
2. Откройте **Settings → API Keys**
3. Заполните:
   - **Bot API URL**: `http://IP:8000/ai_query`
   - **Bot API Key**: полученный ключ
   - **Encryption Key** (опционально для безопасности)
4. Нажмите **🔄 Проверить соединение**
5. **OK** → готово!

---

## ⚙️ Конфигурация

### Расположение файла

**Локальный:** `ApiAi/config_qt.json`  
**Шаблон:** `ApiAi/config/config_qt.json.template`

### Структура config_qt.json

```json
{
  "api_keys": {
    "anthropic": "",
    "openai": "",
    "ollama_url": "http://localhost:11434",
    "telegram_url": "http://YOUR_SERVER_IP:8000/ai_query",
    "telegram_key": "",
    "telegram_enc_key": "",
    "telegram_use_encryption": true,
    "app_id": "apiai-v1"
  },
  "app_info": {
    "version": "1.0.0",
    "name": "ApiAi"
  }
}
```

### Параметры API

| Параметр | Описание | Обязательный |
|----------|----------|--------------||
| `anthropic` | Anthropic Claude API ключ | Для использования Claude |
| `openai` | OpenAI GPT API ключ | Для использования GPT |
| `telegram_url` | URL TelegramHelper API | Для Telegram Bot |
| `telegram_key` | API ключ (64 символа hex) | Для Telegram Bot |
| `telegram_enc_key` | Ключ шифрования | Опционально |
| `telegram_use_encryption` | Включить шифрование | По умолчанию: true |
| `app_id` | ID приложения | По умолчанию: apiai-v1 |

---

## 🤖 Провайдеры API

### 1. Anthropic Claude

**Получение ключа:**
1. Откройте [console.anthropic.com](https://console.anthropic.com/)
2. Создайте аккаунт
3. API Keys → Create Key
4. Скопируйте ключ (начинается с `sk-ant-`)

**Настройка:**
```json
{
  "api_keys": {
    "anthropic": "sk-ant-api03-..."
  }
}
```

**Модель:** Claude 3.5 Sonnet

### 2. OpenAI GPT

**Получение ключа:**
1. Откройте [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Create new secret key
3. Скопируйте ключ (начинается с `sk-`)

**Настройка:**
```json
{
  "api_keys": {
    "openai": "sk-proj-..."
  }
}
```

**Модель:** GPT-4o

---

## 🔗 Telegram Bot API

### Получение доступа

**Через Telegram бота:**
1. Найдите вашего бота в Telegram
2. Отправьте команду: `/api` (только для админа)
3. Скопируйте URL и ключ из ответа

**Ответ бота:**
```
🔐 API для ApiAi

📍 URL: http://YOUR_SERVER_IP:8000/ai_query
🔑 API Key: 754c7afb2b146882...

Как использовать:
1. Откройте ApiAi
2. Settings → API Keys
3. Вставьте URL и ключ выше
4. Нажмите "Проверить соединение"
```

### Настройка в ApiAi

```json
{
  "api_keys": {
    "telegram_url": "http://YOUR_SERVER_IP:8000/ai_query",
    "telegram_key": "754c7afb2b146882181bac0af01f21607158e3ffba8d5853628364b61101464c",
    "telegram_use_encryption": true,
    "telegram_enc_key": "a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef12345678",
    "app_id": "apiai-v1"
  }
}
```

### Параметры безопасности

| Параметр | Значение | Описание |
|----------|----------|----------|
| `app_id` | `apiai-v1` | ID приложения (должен быть в whitelist сервера) |
| `telegram_use_encryption` | `true/false` | Включить end-to-end шифрование |
| `telegram_enc_key` | hex string | Ключ шифрования (32 байта hex) |

---

## 🔐 Шифрование

### Зачем нужно шифрование?

✅ **Включайте, если:**
- Сервер не использует HTTPS
- Нужна дополнительная защита данных
- Передаются конфиденциальные запросы

❌ **Можно выключить, если:**
- Сервер использует HTTPS с валидным сертификатом
- Нужна максимальная производительность

### Генерация ключа шифрования

```bash
# Генерация 32-байтного ключа (64 символа hex)
python3 -c "import secrets; print(secrets.token_hex(32))"

# Или через OpenSSL
openssl rand -hex 32
```

**Пример вывода:**
```
a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef12345678
```

### Настройка шифрования

**Через GUI:**
1. Settings → API Keys → Telegram Bot API
2. Включите **"Шифрование"**
3. Вставьте **Encryption Key**
4. **OK**

**Через config:**
```json
{
  "api_keys": {
    "telegram_use_encryption": true,
    "telegram_enc_key": "ваш_ключ_64_символа"
  }
}
```

### Алгоритмы

ApiAi использует:
- **AES-256-GCM** (основной) - Authenticated Encryption
- **Fernet** (fallback) - Symmetric encryption

---

## 🧪 Тестирование

### Проверка через GUI

1. Settings → API Keys
2. Заполните настройки Telegram Bot
3. Нажмите **"🔄 Проверить соединение"**

**Успешный результат:**
```
✅ Соединение успешно установлено!
🔐 Шифрование работает корректно.
```

**Ошибка:**
```
❌ Ошибка при отправке:
401 Unauthorized
```

### Проверка доступности сервера

```bash
# curl (macOS/Linux)
curl http://YOUR_SERVER_IP:8000/health

# PowerShell (Windows)
Invoke-WebRequest -Uri "http://YOUR_SERVER_IP:8000/health"
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "timestamp": "2025-11-30T04:00:00Z",
  "anthropic_available": true,
  "openai_available": true
}
```

---

## 🆘 Устранение проблем

### ❌ "Не задан API ключ"

**Причина:** Ключ не заполнен для выбранного провайдера

**Решение:**
1. Откройте Settings → API Keys
2. Заполните ключ для выбранного провайдера
3. Сохраните (OK)

### ❌ "Connection refused"

**Причина:** API сервер не запущен или недоступен

**Решение:**
1. Проверьте URL (должен быть `http://IP:PORT/ai_query`)
2. Проверьте доступность: `ping YOUR_SERVER_IP`
3. На сервере: `docker ps | grep telegram-api`

### ❌ "401 Unauthorized"

**Причина:** Неверный API ключ

**Решение для Telegram Bot:**
```bash
# Получить актуальный ключ
# В Telegram боте: /api

# Обновить в ApiAi:
# Settings → API Keys → Bot API Key
```

**Решение для Anthropic/OpenAI:**
- Проверьте правильность ключа
- Убедитесь что у аккаунта есть кредиты

### ❌ "Timeout"

**Причина:** Медленное соединение или сервер перегружен

**Решение:**
1. Проверьте скорость интернета
2. Попробуйте другого провайдера AI
3. Увеличьте timeout в коде (если есть доступ)

### ❌ "Ошибка расшифровки ответа"

**Причина:** Неверный ключ шифрования или сервер его не поддерживает

**Решение:**
1. Проверьте `telegram_enc_key` в config
2. Убедитесь что сервер использует тот же ключ
3. Или выключите шифрование: `telegram_use_encryption: false`

### ❌ "Модуль шифрования недоступен"

**Причина:** Не установлена библиотека `cryptography`

**Решение:**
```bash
pip install cryptography
```

---

## 📚 Связанная документация

- [AI_INTEGRATION_GUIDE.md](AI_INTEGRATION_GUIDE.md) — Полное руководство по AI интеграции
- [VERSION_MANAGEMENT.md](VERSION_MANAGEMENT.md) — Управление версиями
- [README.md](../README.md) — Основная документация

---

## 🔑 Краткая шпаргалка

```bash
# Генерация ключа шифрования
python3 -c "import secrets; print(secrets.token_hex(32))"

# Проверка доступности сервера
curl http://YOUR_SERVER_IP:8000/health

# Получение API ключа от бота
# В Telegram: /api
```

**Конфигурация (минимальная):**
```json
{
  "api_keys": {
    "telegram_url": "http://IP:8000/ai_query",
    "telegram_key": "ваш_ключ",
    "telegram_use_encryption": false
  }
}
```

---

*Версия документа: 1.0*  
*Автор: Kurein M.N.*  
*Последнее обновление: 30.11.2025*
