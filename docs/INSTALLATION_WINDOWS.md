# 🪟 Установка ApiAi на Windows

Подробная инструкция по переносу проекта с GitHub и установке на Windows.

---

## ⚡ Быстрый старт (для опытных пользователей)

### Чек-лист установки

1. **Проверка инструментов:**
```cmd
node --version    # Должно быть v16+
npm --version
rustc --version   # Если нет - установите через rustup-init.exe
```

2. **Установка зависимостей:**
```cmd
cd tauri-app
npm install
```

3. **Первый запуск:**
```cmd
npm run tauri dev
```

**Примечание:** Первая компиляция Rust может занять 10-15 минут.

4. **Настройка:**
- Откройте Settings (⚙️)
- Введите PIN: `1234`
- Настройте AI провайдера
- Сохраните настройки

**Расположение конфигурации:**
```
C:\Users\ВАШ_USERNAME\AppData\Roaming\com.apiai.app\config.json
```

**Быстрый доступ (PowerShell):**
```powershell
cd $env:APPDATA\com.apiai.app
```

---

## 📋 Требования

Перед установкой убедитесь, что у вас установлены:

- **Windows** 10/11 (64-bit)
- **Git** ([скачать](https://git-scm.com/download/win))
- **Node.js** v16+ ([скачать](https://nodejs.org))
- **Rust** (установится автоматически через rustup)
- **Visual Studio Build Tools** или **Microsoft C++ Build Tools** (для компиляции Rust)

---

## 🚀 Шаг 1: Установка необходимых инструментов

### 1.1 Установка Node.js

**Проверка:**
```cmd
node --version
npm --version
```

**Если не установлен:**
1. Скачайте установщик с [nodejs.org](https://nodejs.org) (рекомендуется LTS версия)
2. Запустите установщик и следуйте инструкциям
3. Убедитесь, что опция "Add to PATH" включена

**Или через Chocolatey (если установлен):**
```powershell
choco install nodejs-lts
```

### 1.2 Установка Rust

**Установка через rustup (рекомендуется):**

**Вариант 1: Через PowerShell**
```powershell
# Скачайте и запустите rustup-init.exe
# Или выполните в PowerShell:
Invoke-WebRequest https://win.rustup.rs/x86_64 -OutFile rustup-init.exe
.\rustup-init.exe
```

**Вариант 2: Через браузер**
1. Откройте [rustup.rs](https://rustup.rs) в браузере
2. Скачайте `rustup-init.exe`
3. Запустите установщик
4. Выберите "Default installation" (нажмите Enter)
5. После установки перезапустите терминал

**Проверка установки:**
```cmd
rustc --version
cargo --version
```

**Если нужно обновить:**
```cmd
rustup update stable
```

### 1.3 Установка Visual Studio Build Tools

Rust требует компилятор C++ для сборки нативных библиотек.

**Вариант 1: Microsoft C++ Build Tools (рекомендуется, легче)**
1. Скачайте с [visualstudio.microsoft.com/visual-cpp-build-tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
2. Запустите установщик
3. Выберите "C++ build tools"
4. Установите (займет ~3-6 GB)

**Вариант 2: Visual Studio Community (полная версия)**
1. Скачайте с [visualstudio.microsoft.com](https://visualstudio.microsoft.com/)
2. При установке выберите рабочую нагрузку "Desktop development with C++"

**Проверка:**
После установки перезапустите терминал и попробуйте скомпилировать простой Rust проект.

---

## 📥 Шаг 2: Клонирование проекта с GitHub

### 2.1 Клонирование репозитория

**Через Command Prompt или PowerShell:**
```cmd
# Перейдите в нужную директорию (например, C:\Projects)
cd C:\Projects

# Клонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/ApiAi.git

# Или если используете SSH:
# git clone git@github.com:ВАШ_USERNAME/ApiAi.git

# Перейдите в директорию проекта
cd ApiAi
```

**Через Git Bash (если установлен):**
```bash
cd ~/Projects
git clone https://github.com/ВАШ_USERNAME/ApiAi.git
cd ApiAi
```

### 2.2 Проверка структуры проекта

```cmd
# Убедитесь, что все файлы на месте
dir

# Должны быть папки:
# - tauri-app\     (основное приложение)
# - shared-rs\     (Rust библиотека)
# - python\        (опционально, legacy)
# - docs\          (документация)
```

---

## 🔧 Шаг 3: Установка зависимостей

### 3.1 Установка зависимостей Tauri приложения

```cmd
# Перейдите в директорию Tauri приложения
cd tauri-app

# Установите npm зависимости
npm install
```

**Что устанавливается:**
- `@tauri-apps/cli` - инструменты для разработки Tauri

**Время установки:** ~1-2 минуты

### 3.2 Проверка установки

```cmd
# Проверьте версии
node --version
npm --version
rustc --version
cargo --version

# Проверьте Tauri CLI
npx tauri --version
```

---

## 🎯 Шаг 4: Первый запуск

### 4.1 Запуск в режиме разработки

**Вариант 1: Через npm (рекомендуется для Windows)**
```cmd
cd tauri-app
npm run tauri dev
```

> **⚠️ Важно:** На Windows команда `make` не доступна по умолчанию. Используйте `npm run tauri dev` вместо `make dev`.

**Что происходит:**
1. Компилируется Rust backend (первый раз может занять 10-15 минут)
2. Запускается веб-интерфейс
3. Открывается окно приложения
4. Готово к использованию! 🎉

**Примечание:** При первом запуске Rust компилирует все зависимости, это нормально и займет время. На Windows это может занять больше времени, чем на macOS/Linux.

### 4.2 Если возникли ошибки

**Ошибка: "Unable to find required tools"**
```cmd
# Установите/обновите Rust
rustup update stable

# Очистите кеш
cd tauri-app\src-tauri
cargo clean
cd ..\..
npm run tauri dev
```

**Ошибка: "linking with 'link.exe' failed" или "cannot find -luser32"**
```cmd
# Установите Microsoft C++ Build Tools
# См. Шаг 1.3
# После установки перезапустите терминал
```

**Ошибка: "The system cannot find the path specified"**
```cmd
# Убедитесь, что вы в правильной директории
cd C:\Projects\ApiAi\tauri-app
npm run tauri dev
```

**Ошибка: "npm is not recognized"**
```cmd
# Переустановите Node.js с опцией "Add to PATH"
# Или добавьте вручную в переменные окружения:
# C:\Program Files\nodejs\
```

**Ошибка: "make is not recognized"**
```cmd
# На Windows используйте npm команды вместо make:
npm run tauri dev      # вместо make dev
npm run tauri build    # вместо make build
```

---

## ⚙️ Шаг 5: Настройка приложения

### 5.1 Первоначальная настройка

1. **Запустите приложение** (см. Шаг 4)
2. **Откройте настройки:**
   - Нажмите кнопку **"⚙️ Settings"** в боковой панели
   - Введите PIN (по умолчанию: `1234`)
3. **Настройте провайдера:**
   - Выберите AI провайдера (Telegram/Anthropic/OpenAI)
   - Введите необходимые данные:
     - **Telegram**: URL, Port, API Key
     - **Anthropic**: API Key (из [console.anthropic.com](https://console.anthropic.com/))
     - **OpenAI**: API Key (из [platform.openai.com/api-keys](https://platform.openai.com/api-keys))
4. **Сохраните настройки:**
   - Нажмите **"💾 Save Settings"**

### 5.2 Расположение конфигурации

Конфигурация сохраняется автоматически в:
```
C:\Users\ВАШ_USERNAME\AppData\Roaming\com.apiai.app\config.json
```

**Быстрый доступ:**
```cmd
# В PowerShell
cd $env:APPDATA\com.apiai.app

# В Command Prompt
cd %APPDATA%\com.apiai.app
```

### 5.3 Восстановление конфигурации (опционально)

Если у вас есть бэкап конфигурации:

**Вручную:**
```cmd
# Скопируйте config.json в:
# C:\Users\ВАШ_USERNAME\AppData\Roaming\com.apiai.app\
```

---

## 🏗 Шаг 6: Сборка релизной версии (опционально)

Если хотите собрать установщик для распространения:

```cmd
cd tauri-app

# Сборка релиза (на Windows используйте npm вместо make)
npm run tauri build
```

> **⚠️ Важно для Windows:** Команда `make build` не работает на Windows. Используйте `npm run tauri build` вместо этого.

**Результат:**
- **Windows**: 
  - `tauri-app\src-tauri\target\release\bundle\msi\tauri-app_*.msi` (MSI установщик)
  - `tauri-app\src-tauri\target\release\bundle\nsis\tauri-app_*-setup.exe` (NSIS установщик)
- Установщик готов для распространения!

**Время сборки:** ~10-15 минут (первый раз)

**Примечание:** Для сборки MSI установщика может потребоваться дополнительный инструмент WiX Toolset, но Tauri обычно устанавливает его автоматически.

---

## 📦 Шаг 7: Установка Python компонентов (опционально)

Если нужны Python утилиты (legacy GUI или скрипты):

```cmd
# Перейдите в директорию Python
cd python

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
venv\Scripts\activate

# Установите зависимости
python -m pip install -r requirements.txt

# Проверка установки
python --version
python -m pip list
```

**Запуск Python GUI (если нужно):**
```cmd
venv\Scripts\activate
python main.py
```

**Примечание:** На Windows используйте `python` вместо `python3`. Если команда `python` не работает, попробуйте `py`. Используйте `python -m pip` вместо просто `pip` для большей надежности.

---

## ✅ Проверка установки

### Быстрая проверка

```cmd
# Из корня проекта
cd C:\Projects\ApiAi

# 1. Проверка Node.js
node --version    # Должно быть v16+

# 2. Проверка Rust
rustc --version   # Должна быть последняя stable

# 3. Проверка зависимостей Tauri
cd tauri-app
npm list          # Должен показать @tauri-apps/cli

# 4. Проверка компиляции Rust
cd src-tauri
cargo check       # Должно пройти без ошибок
```

### Тестовый запуск

```cmd
cd tauri-app
npm run tauri dev
```

Если приложение открылось - всё готово! 🎉

---

## 🔄 Обновление проекта

### Обновление кода с GitHub

```cmd
# Из корня проекта
cd C:\Projects\ApiAi

# Получить последние изменения
git pull origin main

# Обновить зависимости (если изменились)
cd tauri-app
npm install

# Пересобрать (если нужно)
cd src-tauri
cargo build
```

### Обновление зависимостей

```cmd
# Обновить npm пакеты
cd tauri-app
npm update

# Обновить Rust зависимости
cd src-tauri
cargo update

# Обновить Rust toolchain
rustup update stable
```

---

## 🧹 Очистка артефактов сборки

Если нужно освободить место на диске:

```cmd
cd tauri-app

# Очистка вручную (на Windows make недоступен)
cd src-tauri
cargo clean

# Очистка shared-rs
cd ..\..\shared-rs
cargo clean
```

**Освобождает:** ~5-10 GB дискового пространства

---

## 🐛 Решение проблем

### Проблема: "Command not found: npm" или "npm is not recognized"

**Решение:**
1. Переустановите Node.js с [nodejs.org](https://nodejs.org)
2. Убедитесь, что опция "Add to PATH" включена
3. Перезапустите терминал
4. Проверьте переменные окружения:
   ```cmd
   echo %PATH%
   ```
   Должен содержать: `C:\Program Files\nodejs\`

### Проблема: "make is not recognized"

**Решение:**
На Windows команда `make` не доступна по умолчанию. Используйте npm команды:
```cmd
# Вместо make dev
npm run tauri dev

# Вместо make build
npm run tauri build

# Вместо make clean
cd src-tauri && cargo clean
```

Если очень нужно make, можно установить через [Chocolatey](https://chocolatey.org/) или [MSYS2](https://www.msys2.org/), но это не обязательно.

### Проблема: "Failed to compile Rust" или ошибки линковки

**Решение:**
```cmd
# Установите Microsoft C++ Build Tools (см. Шаг 1.3)
# После установки перезапустите терминал

# Обновите Rust
rustup update stable

# Очистите кеш
cd tauri-app\src-tauri
cargo clean

# Пересоберите
cargo build
```

### Проблема: "Permission denied" или "Access denied"

**Решение:**
1. Запустите терминал от имени администратора (правый клик → "Run as administrator")
2. Или проверьте права доступа к папке проекта
3. Убедитесь, что антивирус не блокирует выполнение

### Проблема: Приложение не открывается

**Решение:**
1. Проверьте логи в терминале
2. Проверьте консоль браузера (правый клик → Inspect Element)
3. Проверьте конфигурацию: `C:\Users\ВАШ_USERNAME\AppData\Roaming\com.apiai.app\config.json`
4. Проверьте, не блокирует ли Windows Defender или другой антивирус

### Проблема: Медленная компиляция

**Решение:**
```cmd
# Используйте параллельную компиляцию (по умолчанию)
# Увеличьте количество потоков (в PowerShell)
$env:CARGO_BUILD_JOBS=8

# Или используйте sccache для кеширования
cargo install sccache
$env:RUSTC_WRAPPER="sccache"
```

### Проблема: Антивирус блокирует .venv или сборку

**Решение:**
1. Добавьте папку проекта в исключения антивируса
2. Или используйте `venv` вместо `.venv` (как указано в правилах проекта)
3. Это нормально для Windows - антивирус может блокировать виртуальные окружения

### Проблема: Git Bash vs Command Prompt

**Рекомендация:**
- Используйте **PowerShell** или **Command Prompt** для основных команд
- Git Bash может иметь проблемы с путями, используйте осторожно
- Для npm и cargo лучше использовать нативные Windows терминалы

---

## 📚 Дополнительная документация

- **[README.md](../README.md)** - Основная документация проекта
- **[BUILD.md](../BUILD.md)** - Подробное руководство по сборке
- **[docs/PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта
- **[VERSION_MANAGEMENT.md](../VERSION_MANAGEMENT.md)** - Управление версиями
- **[tauri-app/README.md](../tauri-app/README.md)** - Документация Tauri приложения

---

## 🎯 Быстрая справка

### Основные команды

```cmd
# Запуск в режиме разработки
cd tauri-app && npm run tauri dev

# Сборка релиза
cd tauri-app && npm run tauri build

# Очистка артефактов
cd tauri-app\src-tauri && cargo clean
```

### Расположение файлов

- **Конфигурация:** `C:\Users\ВАШ_USERNAME\AppData\Roaming\com.apiai.app\config.json`
- **Логи:** `C:\Users\ВАШ_USERNAME\AppData\Local\com.apiai.desktop\logs\`
- **Установщик:** `tauri-app\src-tauri\target\release\bundle\msi\` или `bundle\nsis\`

### Быстрый доступ к конфигурации

**PowerShell:**
```powershell
cd $env:APPDATA\com.apiai.app
notepad config.json
```

**Command Prompt:**
```cmd
cd %APPDATA%\com.apiai.app
notepad config.json
```

---

## 💡 Полезные советы для Windows

### Использование разных терминалов

- **PowerShell** - рекомендуется для большинства задач
- **Command Prompt (CMD)** - классический терминал Windows
- **Git Bash** - может иметь проблемы с путями, используйте осторожно
- **Windows Terminal** - современный терминал от Microsoft (рекомендуется)

### Переменные окружения

Если команды не находятся, проверьте PATH:
```cmd
echo %PATH%
```

Добавьте в PATH (если нужно):
- `C:\Program Files\nodejs\`
- `C:\Users\ВАШ_USERNAME\.cargo\bin\`

### Антивирус и виртуальные окружения

Windows антивирусы часто блокируют `.venv` папки. Это нормально. Решения:
1. Добавьте папку проекта в исключения
2. Используйте `venv` вместо `.venv` (как указано в правилах проекта)
3. Временно отключите защиту в реальном времени при установке

---

## ✨ Готово!

Теперь у вас установлен и настроен ApiAi на Windows! 

**Следующие шаги:**
1. Настройте AI провайдера в Settings
2. Протестируйте соединение через Echo Test
3. Начните использовать приложение!

**Вопросы?** Проверьте документацию в папке `docs/` или создайте issue на GitHub.

---

**Разработчик:** Kurein M.N.  
**Версия:** 2.4.2  
**Последнее обновление:** Декабрь 2025
