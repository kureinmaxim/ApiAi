# ⚡ Быстрая установка ApiAi на macOS

Краткая шпаргалка для опытных пользователей.

---

## 📋 Чек-лист установки

### 1. Проверка инструментов
```bash
node --version    # Должно быть v16+
npm --version
rustc --version   # Если нет - установите через rustup
xcode-select -p   # Если нет - установите через xcode-select --install
```

### 2. Клонирование проекта
```bash
cd ~/Projects
git clone https://github.com/ВАШ_USERNAME/ApiAi.git
cd ApiAi
```

### 3. Установка зависимостей
```bash
cd tauri-app
npm install
```

### 4. Первый запуск
```bash
npm run tauri dev
```

**Примечание:** Первая компиляция Rust может занять 5-10 минут.

---

## 🔧 Если что-то пошло не так

### Rust не установлен
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

### Xcode Command Line Tools не установлены
```bash
xcode-select --install
```

### Ошибки компиляции
```bash
cd tauri-app/src-tauri
cargo clean
cd ..
npm run tauri dev
```

---

## ⚙️ Первоначальная настройка

1. Запустите приложение: `cd tauri-app && npm run tauri dev`
2. Откройте Settings (⚙️)
3. Введите PIN: `1234`
4. Настройте AI провайдера
5. Сохраните настройки

---

## 📚 Полная инструкция

См. [INSTALLATION_MACOS.md](INSTALLATION_MACOS.md) для подробной документации.

---

## ✅ Готово!

Теперь можно использовать ApiAi! 🎉

