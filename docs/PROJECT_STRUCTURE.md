# 🏗 Структура проекта ApiAi

Этот документ описывает структуру всего проекта ApiAi, включая все три версии.

## 📂 Общая структура проекта

```
ApiAi/
├── tauri-app/             # 🌐 Tauri версия (современная, рекомендуемая)
├── python/                # 🐍 Python версия (стабильная)
├── rust/                  # 🦀 Rust CLI (экспериментальная)
├── docs/                  # 📚 Документация проекта
│   ├── CHEAT_SHEET.md     # Шпаргалка команд
│   ├── ENCRYPTION.md      # Протокол шифрования
│   ├── PORTS.md           # Сетевые порты
│   └── PROJECT_STRUCTURE.md  # Этот файл
├── BUILD.md               # 🔨 Инструкции по сборке
├── VERSION_MANAGEMENT.md  # 📦 Управление версиями
├── README.md              # 📄 Главная документация
└── config_qt.json         # ⚙️ Общий конфиг (gitignored)
```

---

## 🌐 Tauri версия (`tauri-app/`)

Современная версия с веб-интерфейсом и Rust бэкендом.

### 📂 Структура

```
tauri-app/
├── src/                    # Frontend (Web)
│   ├── index.html         # HTML структура
│   ├── styles.css         # Стили с градиентами
│   └── main.js            # JavaScript логика
├── src-tauri/             # Backend (Rust)
│   ├── src/
│   │   ├── main.rs        # Точка входа Tauri
│   │   ├── lib.rs         # Библиотечный код
│   │   └── api.rs         # API клиент
│   ├── Cargo.toml         # Rust зависимости
│   └── tauri.conf.json    # Конфигурация Tauri
├── package.json           # Node.js зависимости
└── README.md              # Документация Tauri версии
```

### 🎯 Ключевые файлы

#### Frontend

- **`src/index.html`**
  - PIN модальное окно для защиты настроек
  - Sidebar с настройками провайдера
  - Область чата
  - Футер с информацией (разработчик, версия, дата, размер окна)

- **`src/styles.css`**
  - Современный дизайн с purple/indigo градиентами
  - Анимации и transitions
  - Glassmorphism эффекты
  - Стили для защищенных полей

- **`src/main.js`**
  - PIN логика (модальное окно, только для настроек)
  - Управление lock/unlock состоянием
  - Отслеживание размера окна
  - Обработка событий (double-click на developer name)

#### Backend

- **`src-tauri/src/main.rs`**
  - Инициализация Tauri приложения
  - Регистрация команд
  
- **`src-tauri/src/api.rs`**
  - HTTP клиент для AI провайдеров
  - Шифрование для Telegram
  - Обработка запросов/ответов

---

## 🦀 Rust CLI версия (`rust/`)

Экспериментальная командная строка версия.

### 📂 Структура

```
rust/
├── src/
│   ├── main.rs            # Точка входа (CLI)
│   ├── api.rs             # API клиент
│   └── encryption.rs      # Модуль шифрования
├── Cargo.toml             # Манифест проекта
├── Cargo.lock             # Фиксация версий зависимостей
├── Makefile               # Команды управления
└── README.md              # Документация Rust версии
```

### 🦀 Ключевые файлы

#### Конфигурация и Сборка

- **`Cargo.toml`**
  - Метаданные проекта (имя, версия, автор)
  - Список зависимостей (`reqwest`, `serde`, `aes-gcm`)

- **`Cargo.lock`**
  - Автогенерируемый файл
  - Фиксирует точные версии зависимостей
  - **Не редактировать вручную**

- **`Makefile`**
  - Команды для управления версиями
  - Синхронизация конфигов
  - Определяет ОС для выбора Python интерпретатора

#### Исходный код

- **`src/main.rs`**
  - Точка входа в CLI приложение
  - Парсинг аргументов командной строки
  - Инициализация API клиента

- **`src/api.rs`**
  - HTTP клиент для AI провайдеров
  - Поддержка Telegram, Anthropic, OpenAI
  - Шифрование данных (AES-GCM)

- **`src/encryption.rs`**
  - Модуль шифрования/дешифрования
  - AES-256-GCM алгоритм

---

## 🐍 Python версия (`python/`)

Стабильная десктопная версия с PyQt6 GUI.

См. [python/README.md](../python/README.md) для подробной документации.

---

## 🔗 Общие файлы

### 📄 `config_qt.json` (корень проекта)

Единый конфигурационный файл для всех версий:

```json
{
  "app_info": {
    "name": "ApiAi",
    "version": "1.0.5",
    "developer_en": "Maksim Kurein"
  },
  "api_keys": {
    "anthropic": "",
    "openai": "",
    "telegram_url": "http://...",
    "telegram_key": "...",
    "telegram_enc_key": "...",
    "telegram_use_encryption": true
  },
  "ui": {
    "theme": "dark"
  },
  "security": {
    "pin_code": "1234",
    "require_pin": true
  }
}
```

**Примечание:** Файл находится в `.gitignore` и создается автоматически при первом запуске.

### 📂 `docs/` (документация)

Вся документация проекта собрана в одном месте:

- **`CHEAT_SHEET.md`** - Быстрая шпаргалка команд для всех версий
- **`ENCRYPTION.md`** - Протокол шифрования AES-256-GCM
- **`PORTS.md`** - Документация сетевых портов
- **`PROJECT_STRUCTURE.md`** - Этот файл (структура проекта)

### 📄 Корневые документы

- **`README.md`** - Главная документация проекта
- **`BUILD.md`** - Инструкции по сборке, запуску и логам
- **`VERSION_MANAGEMENT.md`** - Руководство по управлению версиями

---

## 🚀 Какую версию использовать?

| Версия | Когда использовать |
|--------|-------------------|
| **Tauri** | Для современного UI, лучшего UX, красивого дизайна |
| **Python** | Для стабильной десктопной версии без веб-технологий |
| **Rust CLI** | Для экспериментов, скриптов, автоматизации |

**Рекомендация:** Используйте **Tauri версию** для лучшего пользовательского опыта.

---

## 📦 Артефакты сборки

### Tauri
- `tauri-app/src-tauri/target/` - Скомпилированные файлы

### Rust CLI
- `rust/target/debug/` - Debug сборка
- `rust/target/release/` - Release сборка (оптимизированная)

**Примечание:** Папки `target/` находятся в `.gitignore` из-за большого размера.
