# 🍎 Установка ApiAi на macOS

Подробная инструкция по переносу проекта с GitHub и установке на новый Mac.

---

## ⚡ Быстрый старт (для опытных пользователей)

### Чек-лист установки

1. **Проверка инструментов:**
```bash
node --version    # Должно быть v16+
npm --version
rustc --version   # Если нет - установите через rustup
xcode-select -p   # Если нет - установите через xcode-select --install
```

2. **Клонирование проекта:**
```bash
cd ~/Projects
git clone https://github.com/ВАШ_USERNAME/ApiAi.git
cd ApiAi
```

3. **Установка зависимостей:**
```bash
cd tauri-app
npm install
```

4. **Первый запуск:**
```bash
npm run tauri dev
```

**Примечание:** Первая компиляция Rust может занять 5-10 минут.

5. **Настройка:**
- Откройте Settings (⚙️)
- Введите PIN: `1234`
- Настройте AI провайдера
- Сохраните настройки

**Расположение конфигурации:**
```
~/Library/Application Support/com.apiai.app/config.json
```

**Быстрый доступ:**
```bash
cd ~/Library/Application\ Support/com.apiai.app
```

---

## 📋 Требования

Перед установкой убедитесь, что у вас установлены:

- **macOS** 10.15+ (Catalina или новее)
- **Git** (обычно предустановлен, проверьте: `git --version`)
- **Node.js** v16+ ([скачать](https://nodejs.org))
- **Rust** (установится автоматически через rustup)
- **Xcode Command Line Tools** (установится автоматически)

---

## 🚀 Шаг 1: Установка необходимых инструментов

### 1.1 Установка Node.js

**Проверка:**
```bash
node --version
npm --version
```

**Если не установлен:**
1. Скачайте с [nodejs.org](https://nodejs.org) (рекомендуется LTS версия)
2. Или через Homebrew:
```bash
brew install node
```

### 1.2 Установка Rust

**Установка через rustup (рекомендуется):**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**После установки:**
```bash
source $HOME/.cargo/env
rustc --version
cargo --version
```

**Если нужно обновить:**
```bash
rustup update stable
```

### 1.3 Установка Xcode Command Line Tools

**Проверка:**
```bash
xcode-select -p
```

**Если не установлены:**
```bash
xcode-select --install
```

---

## 📥 Шаг 2: Клонирование проекта с GitHub

### 2.1 Клонирование репозитория

```bash
# Перейдите в нужную директорию (например, ~/Projects)
cd ~/Projects

# Клонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/ApiAi.git

# Или если используете SSH:
# git clone git@github.com:ВАШ_USERNAME/ApiAi.git

# Перейдите в директорию проекта
cd ApiAi
```

### 2.2 Проверка структуры проекта

```bash
# Убедитесь, что все файлы на месте
ls -la

# Должны быть папки:
# - tauri-app/     (основное приложение)
# - shared-rs/     (Rust библиотека)
# - python/        (опционально, legacy)
# - docs/          (документация)
```

---

## 🔧 Шаг 3: Установка зависимостей

### 3.1 Установка зависимостей Tauri приложения

```bash
# Перейдите в директорию Tauri приложения
cd tauri-app

# Установите npm зависимости
npm install
```

**Что устанавливается:**
- `@tauri-apps/cli` - инструменты для разработки Tauri

**Время установки:** ~1-2 минуты

### 3.2 Проверка установки

```bash
# Проверьте версии
node --version
npm --version
rustc --version
cargo --version

# Проверьте Tauri CLI
npx tauri --version
```

---

## 🎯 Шаг 4: Первый запуск

### 4.1 Запуск в режиме разработки

**Вариант 1: Через Makefile (рекомендуется)**
```bash
cd tauri-app
make dev
```

**Вариант 2: Через npm**
```bash
cd tauri-app
npm run tauri dev
```

**Что происходит:**
1. Компилируется Rust backend (первый раз может занять 5-10 минут)
2. Запускается веб-интерфейс
3. Открывается окно приложения
4. Готово к использованию! 🎉

**Примечание:** При первом запуске Rust компилирует все зависимости, это нормально и займет время.

### 4.2 Если возникли ошибки

**Ошибка: "Unable to find required tools"**
```bash
# Установите/обновите Rust
rustup update stable

# Очистите кеш
cd tauri-app/src-tauri
cargo clean
cd ..
npm run tauri dev
```

**Ошибка: "linking with 'cc' failed"**
```bash
# Установите Xcode Command Line Tools
xcode-select --install
```

**Ошибка: "Permission denied"**
```bash
# Дайте права на выполнение
chmod +x tauri-app/scripts/*.sh
```

---

## ⚙️ Шаг 5: Настройка приложения

### 5.1 Первоначальная настройка

1. **Запустите приложение** (см. Шаг 4)
2. **Откройте настройки:**
   - Нажмите кнопку **"⚙️ Settings"** в боковой панели
   - Введите PIN (по умолчанию: `1234`)
3. **Настройте провайдера:**
   - Выберите AI провайдера (Telegram/Anthropic/OpenAI)
   - Введите необходимые данные:
     - **Telegram**: URL, Port, API Key
     - **Anthropic**: API Key (из [console.anthropic.com](https://console.anthropic.com/))
     - **OpenAI**: API Key (из [platform.openai.com/api-keys](https://platform.openai.com/api-keys))
4. **Сохраните настройки:**
   - Нажмите **"💾 Save Settings"**

### 5.2 Расположение конфигурации

Конфигурация сохраняется автоматически в:
```
~/Library/Application Support/com.apiai.app/config.json
```

**Быстрый доступ:**
```bash
cd ~/Library/Application\ Support/com.apiai.app
```

### 5.3 Восстановление конфигурации (опционально)

Если у вас есть бэкап конфигурации:

```bash
cd tauri-app

# Восстановить из шаблона
make config-restore

# Или восстановить из бэкапа
./scripts/restore_config.sh
```

---

## 🏗 Шаг 6: Сборка релизной версии (опционально)

Если хотите собрать установщик для распространения:

```bash
cd tauri-app

# Сборка релиза
make build

# Или через npm
npm run tauri build
```

**Результат:**
- **macOS**: `tauri-app/src-tauri/target/release/bundle/dmg/ApiAi_*.dmg`
- Установщик готов для распространения!

**Время сборки:** ~5-10 минут (первый раз)

---

## 📦 Шаг 7: Установка Python компонентов (опционально)

Если нужны Python утилиты (legacy GUI или скрипты):

```bash
# Перейдите в директорию Python
cd python

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Установите зависимости
python3 -m pip install -r requirements.txt

# Проверка установки
python3 --version
python3 -m pip list | cat
```

**Запуск Python GUI (если нужно):**
```bash
source venv/bin/activate
python3 main.py
```

**Примечание:** Используйте `python3 -m pip` вместо просто `pip` для большей надежности.

---

## ✅ Проверка установки

### Быстрая проверка

```bash
# Из корня проекта
cd ~/Projects/ApiAi

# 1. Проверка Node.js
node --version    # Должно быть v16+

# 2. Проверка Rust
rustc --version   # Должна быть последняя stable

# 3. Проверка зависимостей Tauri
cd tauri-app
npm list          # Должен показать @tauri-apps/cli

# 4. Проверка компиляции Rust
cd src-tauri
cargo check       # Должно пройти без ошибок
```

### Тестовый запуск

```bash
cd tauri-app
npm run tauri dev
```

Если приложение открылось - всё готово! 🎉

---

## 🔄 Обновление проекта

### Обновление кода с GitHub

```bash
# Из корня проекта
cd ~/Projects/ApiAi

# Получить последние изменения
git pull origin main

# Обновить зависимости (если изменились)
cd tauri-app
npm install

# Пересобрать (если нужно)
cd src-tauri
cargo build
```

### Обновление зависимостей

```bash
# Обновить npm пакеты
cd tauri-app
npm update

# Обновить Rust зависимости
cd src-tauri
cargo update

# Обновить Rust toolchain
rustup update stable
```

---

## 🧹 Очистка артефактов сборки

Если нужно освободить место на диске:

```bash
cd tauri-app

# Очистка через Makefile
make clean

# Или вручную
cd src-tauri
cargo clean

# Очистка shared-rs
cd ../../shared-rs
cargo clean
```

**Освобождает:** ~5-10 GB дискового пространства

---

## 🐛 Решение проблем

### Проблема: "Command not found: npm"

**Решение:**
```bash
# Установите Node.js
brew install node

# Или скачайте с nodejs.org
```

### Проблема: "Failed to compile Rust"

**Решение:**
```bash
# Обновите Rust
rustup update stable

# Очистите кеш
cd tauri-app/src-tauri
cargo clean

# Пересоберите
cargo build
```

### Проблема: "Permission denied" при запуске

**Решение:**
```bash
# Дайте права на выполнение
chmod +x tauri-app/scripts/*.sh

# Или запустите через sudo (не рекомендуется)
```

### Проблема: Приложение не открывается

**Решение:**
1. Проверьте логи в терминале
2. Проверьте консоль браузера (правый клик → Inspect Element)
3. Проверьте конфигурацию: `~/Library/Application Support/com.apiai.app/config.json`

### Проблема: Медленная компиляция

**Решение:**
```bash
# Используйте параллельную компиляцию (по умолчанию)
# Увеличьте количество потоков
export CARGO_BUILD_JOBS=8

# Или используйте sccache для кеширования
cargo install sccache
export RUSTC_WRAPPER=sccache
```

---

## 📚 Дополнительная документация

- **[README.md](../README.md)** - Основная документация проекта
- **[BUILD.md](../BUILD.md)** - Подробное руководство по сборке
- **[docs/PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта
- **[VERSION_MANAGEMENT.md](../VERSION_MANAGEMENT.md)** - Управление версиями
- **[tauri-app/README.md](../tauri-app/README.md)** - Документация Tauri приложения

---

## 🎯 Быстрая справка

### Основные команды

```bash
# Запуск в режиме разработки
cd tauri-app && npm run tauri dev

# Сборка релиза
cd tauri-app && npm run tauri build

# Очистка артефактов
cd tauri-app && make clean

# Проверка версии
cd tauri-app && make version-status

# Обновление версии
cd tauri-app && make version-bump-patch
```

### Расположение файлов

- **Конфигурация:** `~/Library/Application Support/com.apiai.app/config.json`
- **Логи:** `~/Library/Logs/com.apiai.desktop/`
- **Установщик:** `tauri-app/src-tauri/target/release/bundle/dmg/`

---

## ✨ Готово!

Теперь у вас установлен и настроен ApiAi на вашем Mac! 

**Следующие шаги:**
1. Настройте AI провайдера в Settings
2. Протестируйте соединение через Echo Test
3. Начните использовать приложение!

**Вопросы?** Проверьте документацию в папке `docs/` или создайте issue на GitHub.

---

**Разработчик:** Kurein M.N.  
**Версия:** 2.4.2  
**Последнее обновление:** Декабрь 2025
