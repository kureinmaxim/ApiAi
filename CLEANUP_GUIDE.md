# Очистка проекта от временных файлов

## 📊 Проблема

Проект может занимать **5+ GB** из-за временных файлов сборки Rust и Node.js.

## 🔍 Что занимает место

| Папка | Размер | Можно удалить? |
|-------|--------|----------------|
| `tauri-app/src-tauri/target/` | ~5.4 GB | ✅ Да |
| `tauri-app/node_modules/` | ~300 MB | ✅ Да |
| `shared-rs/target/` | ~1.0 GB | ✅ Да |
| Исходный код | ~50 MB | ❌ Нет |

## ✅ Быстрая очистка

### Использовать Makefile (рекомендуется)

```bash
cd tauri-app
make clean
```

**Что будет удалено:**
- `src-tauri/target/` (Rust build artifacts)
- `node_modules/` (Node dependencies)

**Результат:** Проект уменьшится до **~50 MB** (только исходники).

### Вручную

**Tauri Backend:**
```bash
cd tauri-app/src-tauri
cargo clean
```

**Shared Library:**
```bash
cd shared-rs
cargo clean
```

**Node Modules:**
```bash
cd tauri-app
rm -rf node_modules
```

## ❓ Частые вопросы

### Безопасно ли удалять target/?
**Да.** Эти папки содержат только результаты компиляции. При следующем запуске `npm run tauri dev` они будут созданы заново.

### Безопасно ли удалять node_modules/?
**Да.** Это зависимости. Их можно восстановить командой `npm install`.

### Как долго пересоздаются файлы?
- **Rust (target):** Первая сборка займет **3-5 минут**.
- **Node (node_modules):** Установка займет **~1 минуту**.

### Нужно ли это делать регулярно?
Рекомендуется делать это:
1. Если заканчивается место на диске
2. Если возникают странные ошибки сборки
3. Перед архивацией проекта

## 🎯 Рекомендуемая процедура

1. **Очистка:**
   ```bash
   cd tauri-app
   make clean
   ```

2. **Восстановление (когда нужно работать):**
   ```bash
   cd tauri-app
   npm install
   npm run tauri dev
   ```

---

> **💡 Совет:** Используйте `make clean` если проект стал занимать слишком много места или если вы хотите "начать с чистого листа".
